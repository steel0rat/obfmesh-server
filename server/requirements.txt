# obfmesh server runtime, Python 3.11+ (target: Ubuntu 24.04 / Python 3.12).
# uvicorn is installed without the [standard] extra on purpose: that extra pulls python-dotenv
# and watchfiles, which are not wanted on the box. uvloop and httptools are picked up
# automatically by uvicorn when present.
fastapi>=0.115,<0.117
pydantic>=2.7,<3
uvicorn>=0.30,<0.36
uvloop>=0.19,<0.22
httptools>=0.6,<0.7
