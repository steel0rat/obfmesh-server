# Установка obfmesh с нуля

Установка состоит из двух половин: управляющий сервер на Linux и клиент на роутере OpenWrt.
Порядок жёсткий — сначала сервер целиком, потом роутер: клиенту неоткуда взять бандл, пока
сервер не отвечает.

Что читать рядом: [SPEC.md](SPEC.md) — контракт, любое расхождение с ним ошибка;
[DEPLOY.md](DEPLOY.md) — тот же выкат, но пошагово с откатом на каждом шаге, для боевых машин
`45.136.127.10` и `192.168.2.1`; [PACKAGING.md](PACKAGING.md) — как собрать пакеты, которые тут
устанавливаются.

Обозначения те же, что в DEPLOY.md: `[S]` — блок команд выполняется на сервере под root,
`[R]` — на роутере под root.

Ни один шаг на роутере не трогает главную таблицу маршрутизации, firewall и конфигурацию
существующих обходных средств. obfmesh создаёт свои интерфейсы `owg{i}`, свои таблицы маршрутов
`51820+i` и свои правила с приоритетом `32000+i`. Пока потребители не привязаны к лучам
(шаг 3.7), для домашней сети установка незаметна.

Схема с версии 1.2.0 симметрична: **N независимых лучей**, у каждого свой адрес, своя таблица и
своё правило. Ни `agg0`, ни VRF, ни `teql`, ни агрегатного адреса больше нет — почему, написано в
SPEC.md, раздел «Что проверено и отвергнуто».

---

## 1. Что нужно и как проверить готовность

### 1.1 Сервер

| Что | Зачем | Минимум |
|---|---|---|
| Ubuntu 24.04 / Debian 12, root | systemd-юнит, управление сетевым стеком | — |
| Python | сам сервис | 3.11+ |
| `wireguard-tools`, `iproute2`, `iptables` | `wg`, `ip`, правила NAT | — |
| Модуль ядра `wireguard` | интерфейсы `swg{i}` | в составе ядра ≥5.6 |
| `wg-obfuscator` | обфускация | 1.6+, с поддержкой `masking` |
| Обратный прокси с TLS | публикация API наружу | Caddy или nginx |
| Внешние порты `udp/48201..4820N` | приём лучей | по числу лучей |
| Свободный `tcp/8080` на 127.0.0.1 | API | — |

Проверка `[S]`:

```sh
. /etc/os-release; echo "$PRETTY_NAME"
python3 -V
command -v wg ip iptables systemctl || echo 'ЧЕГО-ТО НЕТ'
modprobe wireguard && ls -d /sys/module/wireguard
ss -lntp | grep -E ':(443|8080)\b' || echo 'порты 443 и 8080 свободны'
```

Ожидается: Python 3.11 и выше, все четыре утилиты на месте, модуль загружается, порты свободны.
Если Python старше — дальше идти нельзя, сервис не запустится.

### 1.2 Роутер

| Что | Зачем |
|---|---|
| OpenWrt/FriendlyWrt 25.12.x, aarch64, procd, BusyBox ash, `apk` | целевая платформа |
| `wireguard-tools`, `kmod-wireguard` | интерфейсы `owg{i}` |
| `curl`, `jsonfilter` | REST, SSE, разбор бандла |
| `ip-full` | `ip rule` и `ip -d link` — в `ip-tiny` их нет |
| `wg-obfuscator` | той же версии, что на сервере |
| ~1 МБ свободной флеш-памяти | пакеты `obfmesh` и `luci-app-obfmesh` |

`kmod-vrf` и `kmod-sched-teql` в 1.2.0 не нужны: общих устройств не осталось, удалять их не
обязательно, но и держать незачем.

`tc-full` — отдельный случай. Зависимостью он больше не является: obfmesh ничего не вешает на
`tc`. Но роутер, обновляемый **с 1.1.0 в режиме `teql`**, приходит с teql-дисциплиной на лучах,
и снять её умеет только `tc`. Обнаруживает её obfmesh через `ip link` (он в зависимостях), а
если `tc` нет — удаляет устройство луча, и тот же apply создаёт его заново уже чистым. Оба пути
рабочие; с `tc` он тише. После первого удачного apply `tc-full` можно снимать.

`taskset` в зависимостях тоже нет, потому что это applet BusyBox на одних сборках и пакет
`util-linux-taskset` на других. Без него привязка обфускаторов к ядрам пропускается — а это
измеренные 359 → 210 Мбит/с, так что проверить стоит: `command -v taskset`.

Необязательный пакет obfmesh-balance (раздел 3.8) добавляет к этому списку `nftables` — и больше
ничего. Ни одного `kmod-nft-*` он не требует, и это сделано намеренно.

Необязательный пакет obfmesh-onwatch (раздел 3.9) добавляет `tcpdump`: имена целей он берёт только
пассивным захватом DNS на петле, других источников у него нет. `nftables`, `jsonfilter`, `ip-full`
и `curl` к тому моменту уже стоят, а `kmod-nft-*` он не требует по той же причине, что и сосед.

**Ловушка, на которой уже обожглись.** На FriendlyWrt работает ядро 6.1.141 из BSP FriendlyElec
(`uname -r`), а пакеты `kmod-*` в её feed собраны под 6.12.94. `apk add kmod-nft-core` либо не
проходит вовсе, либо кладёт на флеш модули, которые это ядро не загрузит: vermagic не совпадает.
Ровно так в августе не встал `kmod-amneziawg`. Ставить `kmod-*` на этой прошивке нельзя, и
незачем: нужные модули уже в образе — `/lib/modules/6.1.141/nft_numgen.ko` лежит на месте, а
`nf_tables` и NAT подняты фаерволом fw4.

Проверяется поэтому не наличие пакета, а способность работающего ядра сделать то, что нужно
(модуль подгружается по требованию):

```sh
uname -r                                       # 6.1.141 — ядро FriendlyElec
ls /lib/modules/$(uname -r)/nft_numgen.ko      # модуль в прошивке
nft -c -f - <<'EOF'
table inet omb_probe {
	chain c {
		meta mark set numgen inc mod 100
	}
}
EOF
```

Молчание `nft -c -f` — всё на месте. Ту же пробу делают `install-nosdk.sh` перед установкой и
`obfmesh-balance check` при каждом запуске, причём совет в обоих один и тот же слово в слово:
смотреть `/lib/modules/$(uname -r)/nft_numgen.ko`, а не ставить `kmod-nft-core`.

Проверка `[R]`:

```sh
cat /etc/openwrt_release | head -3; uname -r
apk info -e wireguard-tools kmod-wireguard curl jsonfilter ip-full
ls -l /usr/bin/wg-obfuscator
ip route show table 51821; ip route show table 51822     # должно быть пусто
ip rule show | grep -E '^320[0-9][0-9]:' || echo 'правила свободны'
nproc; df -h /overlay | tail -1
```

Чего не хватает — ставится одной командой:

```sh
apk add ip-full
ip -V; readlink -f /sbin/ip     # ip-full приоритетнее ip-tiny в alternatives
```

`ip-full` и `ip-tiny` в 25.12 сосуществуют: `/sbin/ip` — симлинк, который система alternatives
переключает на `ip-full` (приоритет 300 против 200). Роутер не остаётся без `ip` ни на секунду.

Если таблицы `51821+` или приоритеты `32001+` уже заняты чужими правилами — задайте другие базы
на шаге 3.3 (`route_table_base`, `rule_pref_base`). Чужие маршруты obfmesh не трогает, но и делить
таблицу с кем-то не будет: уборка луча стирает таблицу целиком. Сменить базу можно и потом, на
работающем роутере: со следующим `apply` пара правил переезжает на новые приоритеты, а прежняя
снимается — обе формы obfmesh узнаёт по селектору, а не по номеру.

### 1.3 Обфускатор

`wg-obfuscator` нужен с обеих сторон и в пакеты не входит: путь задаётся настройкой
(`obfuscator_bin` в uci на роутере, `PATCH /api/settings` на сервере).

```sh
wg-obfuscator --version
wg-obfuscator --help | grep -i masking
```

В выводе `--help` обязано быть `masking`, среди значений — `STUN`. Без маскировки провайдер режет
поток, а выглядит это как «туннель поднят, рукопожатие есть, трафика нет». obfmesh проверяет это
сам и отказывается поднимать луч на бинарнике без `masking`.

Значение маскировки должно совпадать на обоих концах байт в байт — обе стороны пишут `STUN`
заглавными.

---

## 2. Сервер

### 2.1 Бинарник обфускатора

```sh
install -m 0755 wg-obfuscator /usr/local/bin/wg-obfuscator
/usr/local/bin/wg-obfuscator --help | grep -i masking
```

Путь произвольный, но его надо запомнить: он попадёт в настройки.

### 2.2 Управляющий сервис

Два способа, результат одинаковый. Пакет удобнее там, где нужны обновления и удаление одной
командой; `install.sh` — там, где сервер один и ставится из исходников.

**Способ А, пакет** (сборка описана в [PACKAGING.md](PACKAGING.md)):

```sh
OBFMESH_EXTERNAL_HOST=45.136.127.10 \
OBFMESH_OBFUSCATOR_BIN=/usr/local/bin/wg-obfuscator \
apt-get install -y ./obfmesh-server_1.4.0-1_all.deb
```

Обе переменные необязательны и читаются только при первом создании строки настроек. Без
`OBFMESH_EXTERNAL_HOST` адрес определяется по маршруту по умолчанию — на сервере с одним белым
адресом это обычно верно, но проверить стоит (`obfmesh-ctl status`). Изменить потом:
`PATCH /api/settings`.

Postinst создаёт venv, генерирует админ-ключ, включает и запускает юнит. Если Python-зависимости
поставить не удалось (нет сети и нет вшитых wheels), установка честно падает; после починки сети
— `apt-get -f install`.

**Способ Б, из исходников:**

```sh
apt-get install -y wireguard-tools iproute2 iptables python3-venv
cd server
OBFMESH_EXTERNAL_HOST=45.136.127.10 \
OBFMESH_OBFUSCATOR_BIN=/usr/local/bin/wg-obfuscator \
sudo -E ./install.sh
```

`install.sh` идемпотентен: повторный запуск при неизменных исходниках ничего не перезапускает. В
конце он требует, чтобы каждый настроенный луч был поднят, а его обфускатор жив, — то есть
проверяет не «FastAPI стартовал», а «система сошлась».

Смешивать способы не надо: `install.sh` кладёт юнит в `/etc/systemd/system`, а пакет — в
`/usr/lib/systemd/system`, и копия в `/etc` перекрывает пакетную. Postinst об этом предупреждает.

**Проверка** `[S]`:

```sh
systemctl is-active obfmesh-server
obfmesh-ctl status
wg show swg1 | head -5
ss -lunp | grep -E ':(48201|48202)\b'
pgrep -af wg-obfuscator
```

Ожидается: сервис `active`, два луча (значение по умолчанию), у каждого поднят интерфейс и жив
обфускатор, порты слушаются. Если нет — `journalctl -u obfmesh-server -n 80` и
`tail -50 /var/log/obfmesh/obf1.log`.

### 2.3 Что появилось в системе

| Путь | Что | Права |
|---|---|---|
| `/opt/obfmesh` | код и venv | 755 |
| `/etc/obfmesh/admin.key` | админ-ключ, значение нигде не печатается | 600 |
| `/etc/obfmesh/obf{i}.conf` | конфиги обфускаторов, в них ключ обфускации | 600 |
| `/etc/obfmesh/caddy-snippet.txt` | готовый блок для обратного прокси | 644 |
| `/var/lib/obfmesh/obfmesh.db` | настройки, ключи лучей, клиенты | 700 на каталоге |
| `/run/obfmesh/obf{i}.pid` | pid-файлы, по ним обфускаторы подбираются после рестарта | 700 на каталоге |
| `/var/log/obfmesh/obf{i}.log` | логи обфускаторов, ротация `/etc/logrotate.d/obfmesh` | 700 на каталоге |
| `obfmesh-ctl` | локальное управление без HTTP (`/usr/sbin` из пакета, `/usr/local/sbin` из install.sh) | 755 |

Интерфейсы `swg{i}` с адресом `10.77.{i}.1/24`, правила NAT для `10.77.0.0/16` и `ip_forward=1`
создаёт сам сервис при первом `reconcile()`. Никаких маршрутов к клиенту сервер не ставит: адрес
клиента в луче i лежит в `/24` своего `swg{i}` и попадает туда по AllowedIPs.

### 2.4 Публикация наружу

API слушает только `127.0.0.1:8080`. Это не настройка «по желанию»: без обратного прокси роутер до
сервера не достучится, а TLS взять неоткуда.

**Caddy.** Готовый блок лежит в `/etc/obfmesh/caddy-snippet.txt`:

```
45.136.127.10 {
    handle_path /obfmesh/* {
        reverse_proxy 127.0.0.1:8080 {
            flush_interval -1
        }
    }
}
```

`flush_interval -1` обязателен: `/api/events` — потоковый ответ, буферизация превращает
реактивность в опрос раз в 15 минут.

```sh
caddy validate --config /etc/caddy/Caddyfile
systemctl reload caddy
```

**nginx** — если он уже стоит на сервере:

```
location /obfmesh/ {
    proxy_pass http://127.0.0.1:8080/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_read_timeout 3600s;
}
```

Слэш в конце `proxy_pass` обязателен — он срезает префикс `/obfmesh`, как это делает `handle_path`
у Caddy. Панель строит адреса API относительно текущего пути, поэтому с обоими вариантами она
работает без дополнительных настроек. `proxy_buffering off` — то же самое требование, что
`flush_interval -1`.

**Порты обфускаторов.** `udp/48201..4820N` должны быть доступны снаружи. В юните стоит
`OBFMESH_MANAGE_INPUT=1`, и сервис сам вставляет разрешающие правила в INPUT; если правилами
управляете вы, поставьте `0` в drop-in и откройте порты сами.

**Проверка** `[S]`:

```sh
curl -sk -o /dev/null -w '%{http_code}\n' https://45.136.127.10/obfmesh/api/status
# 401 — прокси работает и аутентификация на месте

KEY=$(cat /etc/obfmesh/admin.key)
curl -sk -H "X-API-Key: $KEY" https://45.136.127.10/obfmesh/api/status | head -c 200
```

С третьей машины — что порт обфускатора виден снаружи:

```sh
nc -u -z -w3 45.136.127.10 48201; echo $?
```

и одновременно на сервере `tcpdump -ni any udp port 48201 -c 5`. Пакеты видны — путь есть.

**Сертификат для голого IP.** Caddy выписывает его своим внутренним CA, и curl на роутере ему не
поверит. Корневой сертификат понадобится на шаге 3.2:

```sh
cp /var/lib/caddy/.local/share/caddy/pki/authorities/local/root.crt /root/obfmesh-ca.crt
```

### 2.5 Первый вход в панель

Откройте `https://45.136.127.10/obfmesh/`. Панель спросит админ-ключ — тот самый, что лежит в
`/etc/obfmesh/admin.key`:

```sh
sudo cat /etc/obfmesh/admin.key
```

Галочка «запомнить» кладёт ключ в `localStorage`, без неё — в `sessionStorage` на время вкладки. С
общей машины лучше без галочки.

Три раздела: **Состояние** (лучи, рукопожатия, счётчики), **Настройки лучей** (число лучей,
маскировка, MTU, внешний адрес), **Клиенты**.

### 2.6 Клиент и токен

Через панель: раздел «Клиенты» → имя (латиница, цифры, точка, дефис, подчёркивание) → «Создать».
Токен показывается **один раз**, вместе с готовой строкой `uci set ...` для роутера и кнопкой
скачивания бандла.

То же самое из консоли:

```sh
KEY=$(sudo cat /etc/obfmesh/admin.key)
curl -s -X POST http://127.0.0.1:8080/api/clients \
     -H "X-API-Key: $KEY" -H 'Content-Type: application/json' \
     -d '{"name":"nanopi"}'
```

Токен в открытом виде на сервере не хранится — только его хеш и первые восемь hex-символов
(`token_id`) для сверки «тот ли токен». Потеряли — заведите нового клиента.

Создание клиента сразу прописывает его пиров на все лучи, каждому — ровно один адрес:

```sh
curl -s -H "X-API-Key: $KEY" http://127.0.0.1:8080/api/clients
wg show swg1 allowed-ips        # 10.77.1.2/32 и ничего больше
wg show swg2 allowed-ips        # 10.77.2.2/32 и ничего больше
```

Второй адрес в AllowedIPs пира — след схемы 1.1.0 с агрегатным адресом. На 1.2.0 его быть не
должно; появился — это ошибка реализации, а не настройка.

---

## 3. Роутер

### 3.1 Пакеты

Ставится **установщиком из исходников**, а не пакетом. Собранного `.apk` в репозитории нет: пакет
собирается SDK OpenWrt, а это Linux x86_64 (PACKAGING.md, 2.1). Установщик кладёт ровно те же файлы,
что положил бы пакет, поэтому будущее обновление пакетом заменит их начисто.

С рабочей машины — исходники на роутер:

```sh
scp -r openwrt/obfmesh root@192.168.2.1:/root/     # каталог целиком: установщику нужен files/
```

На роутере:

```sh
sh /root/obfmesh/install-nosdk.sh
install -m 0755 wg-obfuscator /usr/bin/wg-obfuscator
/usr/bin/wg-obfuscator --help | grep -i masking
```

Установщик проверяет зависимости (`uci`, `curl`, `jsonfilter`, `wg`, `ip`), не перезаписывает
существующий `/etc/config/obfmesh`, останавливает сервис на время замены файлов и поднимает его
обратно, если он работал. Останов не косметика: `cp` поверх исполняющегося скрипта обрывает его
посреди файла с кодом возврата 0.

Сервис он же и `enable`. Пока не настроены `server_url` и `token`, watcher только пишет об этом в
лог — это ожидаемо.

**Что из этого следует.** Файлы, положенные установщиком, в базе apk не зарегистрированы:

- `apk info -e obfmesh` молчит — это не потеря пакета, а свойство такой установки;
- обновление — повторный запуск установщика с новыми исходниками; `apk upgrade` про эти файлы не
  знает;
- `apk del obfmesh` ничего не удалит: снятие — файлами, 6.1;
- проверки собранного пакета из PACKAGING.md (`apk info -L/-R`, `apk manifest`) к такой установке
  не применимы; вместо них — `ls -l` ниже.

Страница LuCI (`luci-app-obfmesh`) приезжает только пакетом — установщик её не кладёт:

```sh
ls /usr/share/luci/menu.d/luci-app-obfmesh.json 2>/dev/null || echo 'страницы LuCI нет'
```

Без неё obfmesh работает полностью: всё, что делает страница, делает CLI (`obfmesh status`,
`obfmesh spokes N`).

Если на роутере лежал ручной костыль `/usr/lib/obfmesh/tune.sh` (настройка ядер и RPS поверх
1.1.0), уберите его вызов из `rc.local`/cron **до** установки: с 1.2.0 этот файл приходит в
поставке и вызывается самим obfmesh.

**Проверка:**

```sh
ls -l /usr/lib/obfmesh/ /usr/bin/obfmesh      # apply.sh, watcher.sh, tune.sh, lib.sh
obfmesh version
ping -c2 8.8.8.8            # существующий выход в интернет не тронут
```

Обновление с 1.1.0 конфиг не трогает: установщик видит существующий `/etc/config/obfmesh` и
оставляет его как есть, печатая об этом строку. Проверить, что так и вышло:

```sh
grep -c token /etc/config/obfmesh      # токен на месте
ls -l /etc/config/obfmesh              # 600, файл ваш
```

(Установка пакетом, если он когда-нибудь будет собран, обходится с этим файлом так же: он объявлен
в `conffiles`, а образец из пакета ложится рядом как `obfmesh.apk-new` — PACKAGING.md, 2.5.)

### 3.2 Сертификат сервера

Нужен, только если сервер опубликован на голом IP с сертификатом внутреннего CA (шаг 2.4). Для
сертификата от публичного УЦ шаг пропускается: `curl` на OpenWrt использует
`/etc/ssl/certs/ca-certificates.crt`, который приходит вместе с `curl` (зависимость `ca-bundle`).

```sh
scp root@45.136.127.10:/root/obfmesh-ca.crt /etc/obfmesh/server-ca.crt
chmod 600 /etc/obfmesh/server-ca.crt
```

### 3.3 Настройка

```sh
uci set obfmesh.main.server_url='https://45.136.127.10/obfmesh'
uci set obfmesh.main.token='<токен из 2.6>'
uci set obfmesh.main.obfuscator_bin='/usr/bin/wg-obfuscator'
uci set obfmesh.main.ca_file='/etc/obfmesh/server-ca.crt'
uci commit obfmesh
```

Полный список опций с умолчаниями — в приложении В и в комментариях `/etc/config/obfmesh`.

Прежде чем запускать сервис, проверьте, что сервер вообще виден:

```sh
curl --cacert /etc/obfmesh/server-ca.crt -s -o /dev/null -w '%{http_code}\n' \
     https://45.136.127.10/obfmesh/api/status
# 401 — сервер отвечает и сертификат принят
```

`000` — не достучались (адрес, маршрут, firewall). `60` в тексте ошибки curl — сертификат не
принят: не тот CA-файл. Временный обход `uci set obfmesh.main.tls_insecure=1` открывает канал
управления для перехвата, поэтому годится только как способ диагностики.

### 3.4 Первый запуск

```sh
/etc/init.d/obfmesh restart
sleep 15
obfmesh status
```

Что должно быть в выводе:

- `config_version` не пустой, применён секунды назад;
- по каждому лучу `LINK up`, `OBFUSC ok:<pid>`, `HANDSHAKE` меньше двух минут, свой адрес
  `10.77.{i}.2/30`, своя таблица `51820+i`;
- `watcher: running`;
- строки `FAILED` нет;
- в полях `token` и `obfuscation key` — `<есть>`. Значений секретов в выводе быть не должно; если
  значение видно, это ошибка, а не особенность.

### 3.5 Проверка, что трафик ходит

Пока — не трогая домашнюю сеть, а привязываясь к лучу явно:

```sh
ping -c3 -I owg1 10.77.1.1                 # адрес сервера в первом луче
ping -c3 -I owg2 10.77.2.1

curl --interface owg1 -s https://ifconfig.me; echo
curl --interface owg2 -s https://ifconfig.me; echo
# оба должны ответить внешним адресом сервера

wg show owg1 transfer; wg show owg2 transfer
ip -4 addr show owg1                       # 10.77.1.2/30
ip route show table 51821                  # default dev owg1 mtu 1400
ip rule show | grep 5182                   # from 10.77.{i}.2 lookup 51820+i
```

Замер по одному лучу:

```sh
curl --interface owg1 -o /dev/null -s -w 'down %{speed_download} B/s\n' \
     https://speed.hetzner.de/100MB.bin
```

Ожидаемый порядок для одного луча — около 180 Мбит/с (22 МБ/с). Суммарные 359–373 Мбит/с
получаются двумя такими замерами одновременно, каждый со своим `--interface`:

```sh
curl --interface owg1 -o /dev/null -s -w 'owg1 %{speed_download} B/s\n' \
     https://speed.hetzner.de/100MB.bin &
curl --interface owg2 -o /dev/null -s -w 'owg2 %{speed_download} B/s\n' \
     https://speed.hetzner.de/100MB.bin &
wait
```

Один поток по двум лучам не раскладывается — это свойство схемы, а не поломка (SPEC, «Что
проверено и отвергнуто», п. 4).

### 3.6 Настройка роутера

Без неё два луча дают 210 Мбит/с вместо 359–373. С 1.2.0 она приезжает с пакетом
(`/usr/lib/obfmesh/tune.sh`) и применяется идемпотентно при старте сервиса и после каждого
`apply` — pid обфускаторов меняется, привязку надо ставить заново.

Что делается:

1. Обфускаторы прибиваются к старшим ядрам, чтобы не драться за такты с сетевым softirq: на
   4-ядерном R3S с двумя лучами это CPU2 и CPU3 (`taskset -p 4 <pid1>`, `taskset -p 8 <pid2>`).
2. RPS на всех приёмных очередях физических интерфейсов — и **мимо ядер, занятых обфускаторами**:
   `echo 3 > /sys/class/net/<dev>/queues/rx-*/rps_cpus`.
3. `sysctl net.core.netdev_budget=600`.
4. Governor `performance` на всех ядрах.

Проверка `[R]`:

```sh
taskset -p "$(cat /var/run/obfmesh/obf-c1.pid)"    # current affinity mask: 4
taskset -p "$(cat /var/run/obfmesh/obf-c2.pid)"    # current affinity mask: 8
cat /sys/class/net/eth0/queues/rx-0/rps_cpus       # 3 — приём на cpu0-1, мимо обфускаторов
sysctl net.core.netdev_budget                      # 600
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor   # performance
```

**Про маску RPS: `tune_rps_cpus` по умолчанию пуст, и это не «все ядра».** Пустое значение значит
«все ядра машины МИНУС те, на которые привязка обфускаторов реально легла»: на четырёхъядерном R3S
с двумя лучами `f` минус `cpu2-3` даёт `3`, то есть приём остаётся на `cpu0-1`, где и так висят
аппаратные прерывания `eth0`/`eth1` (`smp_affinity=3`). Заданная руками маска всегда сильнее
вычисленной; `0` очереди не трогает вовсе. Если свободного ядра не осталось (обфускаторов столько
же, сколько ядер), шаг не выключается молча — в лог уходит строка с причиной.

Почему это отдельно важно. Прибить обфускаторы и оставить RPS на всех ядрах — половина изоляции, и
стоит она около пятой части полосы. Один и тот же луч, переставленный по ядрам (8 потоков по
100 МБ, замер с клиента):

| Ядро обфускатора | Скорость | Накоплено NET_RX softirq на этом ядре |
|---|---|---|
| cpu0 | 254 Мбит/с | 88 млн |
| cpu1 | 250 Мбит/с | 140 млн |
| cpu2 | 248 Мбит/с | 123 млн |
| cpu3 | **342 Мбит/с** | 38 млн |

Быстрым было не ядро, а место, куда RPS почти не заносил приём: обфускаторы меняли местами, и 342
уезжали вместе с `cpu3`, а не вместе с лучом. Частоты у всех четырёх одинаковые (1800 МГц,
`performance`), аппаратные прерывания и до того сидели на `cpu0-1` — асимметрия шла ровно за
маской RPS. После маски `3` раскладка 50/50 перестала прыгать — 318 и 364 в соседних прогонах до,
377 и 382 после, — а лучший итог с балансировкой стал **411 Мбит/с**. Это уже потолок процессора:
под нагрузкой idle 2 %, sirq 53 %, sys 44 %.

Маска живёт не вечно: её сбрасывают редкие сетевые события — перерегистрация устройства, чужой
`sysctl -p`. Постоянного стороннего писателя нет, значение держится сколько угодно, пока его не
сбили, а сбитое возвращает watcher на своём тике — окно расхождения до шести минут.

Выключается настройкой `uci set obfmesh.main.tune=0` — например, если ядра распределяет другой
сервис. Тогда 359 Мбит/с ждать не стоит: измерения выше сняты с ней.

Числа рассчитаны на 4 ядра R3S: обфускаторы на старших ядрах, сетевой softirq на младших. Маску RPS
пакет пересчитает сам под любое число ядер, а вот `tune_obf_cpus` и сами замеры на другом железе
придётся пересчитывать руками, иначе прирост не воспроизведётся.

### 3.7 Разведение сервисов по лучам

Первый шаг, который реально влияет на домашнюю сеть. Делайте, когда предыдущие пункты стабильно
отработали хотя бы сутки.

Именно здесь и получается агрегация. Ядро один поток по лучам не раскладывает, поэтому суммарная
скорость набирается тем, что **разные сервисы уходят в разные лучи**. Раскладка живёт у
потребителя, одной строкой на секцию.

Перед началом — точка отката. Точка отката — это **копия файла** `/etc/config/forkop`: вернуть
конфигурацию можно только тем, что кладётся обратно на своё место как есть.

```sh
cp /etc/config/network /etc/config/network.before-obfmesh

SNAP=/root/forkop.bak.$(date +%Y%m%d-%H%M%S)
( umask 077; cp /etc/config/forkop "$SNAP" )
chmod 600 "$SNAP"
ls -l "$SNAP"                              # -rw-------
md5sum /etc/config/forkop "$SNAP"          # два хеша обязаны совпасть
grep -cE '^config ' "$SNAP"                # сколько секций в снимке — понадобится при откате
echo "$SNAP"                               # запишите имя
```

**`uci show forkop` точкой отката не является.** Он печатает `forkop.google.outbound_json=...` —
плоские строки, по одной на опцию. Такой файл нельзя вернуть копированием в `/etc/config/forkop`:
там нужны `config`, `option` и `list`, и Forkop с LuCI прочитают подсунутый вывод `uci show` как
пустую или битую конфигурацию. По нему же не отличить `list` с одним элементом от `option`, а
разница потом важна (3.8.2). Полезен он другим — читаемостью:

```sh
( umask 077; uci show forkop > "$SNAP.uci" )   # диагностика рядом со снимком, не замена ему
chmod 600 "$SNAP.uci"
```

**Снимок снимается непосредственно перед работой, а не берётся вчерашний.** На этом роутере уже
случилось: файл `/root/forkop.bak.balance-start` снят 22.08 в 19:57, конфигурацию правили 23.08 в
00:04, и в снимке нет целой секции (`kinopub`) и старая форма привязки у `speedtest`. Откат таким
файлом стёр бы секцию вместе с её доменами — молча, потому что удалять нечего: её там просто нет.

**Восстановление по шагам:**

```sh
ls -l /root/forkop.bak.*                                 # выбрать снимок, обычно последний
SNAP=/root/forkop.bak.<дата из имени>

grep -cE '^config ' "$SNAP"                              # секций в снимке
uci show forkop | grep -c '=section$'                    # секций сейчас — числа должны сойтись
grep -E '^config ' "$SNAP"                               # и глазами: все ли секции на месте

uci revert forkop                                        # сбросить незафиксированные правки:
                                                         # иначе первый же commit затрёт снимок
cat "$SNAP" > /etc/config/forkop                         # так целевой файл сохраняет права и inode
md5sum /etc/config/forkop "$SNAP"                        # хеши совпали — файл лёг целиком
uci -q show forkop >/dev/null && echo 'uci файл читает'
/etc/init.d/forkop restart                               # обязателен: копирование файла
                                                         # procd-события не порождает
```

Числа секций не сошлись — снимок устарел, восстанавливать им нельзя: секции, появившейся после
снимка, вы лишитесь. Тогда правится точечно, по одной опции, а снимок остаётся справкой о том, что
в этих опциях было.

**Снимок Forkop — файл с секретом.** В `/etc/config/forkop` есть `subscription_urls` секции,
которая работает по подписке, а это URL с токеном прямо в пути: у кого он есть, тот пользуется
подпиской. Поэтому снимок остаётся на роутере в `/root` и никуда больше: не копируется на рабочую
машину, не кладётся в `/www` (этот каталог отдаётся наружу через проброс 11228) и не вставляется
в переписку. `umask 077` работает только на создание файла — если он уже лежал, права останутся
прежними, поэтому рядом стоит `chmod`.

**Forkop.** Каждая секция привязывается к своему лучу. Смотрим, что есть, и разводим:

```sh
uci show forkop | sed -E "s/^([^=]*(urls?|token|key|secret|password|uuid))=.*/\1='<есть>'/" |
	grep -E 'bind_interface|=.*section'

uci set forkop.<секция_1>.bind_interface='owg1'
uci set forkop.<секция_2>.bind_interface='owg1'
uci set forkop.<секция_3>.bind_interface='owg2'
uci set forkop.<секция_4>.bind_interface='owg2'
```

**Применяет правки `uci commit forkop`, и рвёт соединения тоже он.** В `/etc/init.d/forkop` есть
`procd_add_config_trigger`: commit шлёт procd событие `config.change`, procd перезапускает Forkop
сам, тот пересобирает `/etc/sing-box/config.json` и поднимает sing-box заново. Безусловный
`/etc/init.d/forkop restart` следом — это второй обрыв всех соединений через прокси, и ни за чем.
Порядок такой: commit, пауза, проверка, рестарт — только если проверка показала, что не сработало.

Наблюдать надо за sing-box, а не за Forkop: у `forkop` нет своего procd-инстанса,
`ubus call service list '{"name":"forkop"}'` отвечает пустым объектом и до правки, и после.
Перезапускается `sing-box`, и Forkop сам ждёт смены его pid.

```sh
md5sum /etc/sing-box/config.json     # хеш ДО
pidof sing-box                       # pid sing-box ДО

uci commit forkop
sleep 10                             # триггер стоит на reload с задержкой 2 с, плюс запас

md5sum /etc/sing-box/config.json     # хеш обязан измениться
pidof sing-box                       # pid обязан стать другим
jsonfilter -i /etc/sing-box/config.json -e '@.outbounds[*].bind_interface'   # ожидается пусто
```

Последняя команда — ровно та проверка, которую делает сам балансировщик перед включением,
поэтому её результат и его вердикт не разойдутся.

Как читать результат:

- хеш другой, pid sing-box другой, `bind_interface` пуст — перезапуск состоялся,
  руками ничего делать не надо;
- хеш прежний или в `outbounds` старая привязка — триггер не сработал или собрал конфигурацию до
  правки: только тогда `/etc/init.d/forkop restart` и те же команды ещё раз;
- хеш другой, а pid прежний — конфигурацию пересобрали, но перечитать её некому: sing-box читает
  её только при старте. Тоже `/etc/init.d/forkop restart`.

Привязка бывает в двух формах: отдельной опцией секции, как в примере выше, и полем
`bind_interface` внутри `outbound_json`. Какая стоит сейчас, видно в выводе первой команды; менять
надо ту, что уже есть, а не заводить вторую рядом. Сколько секций и как они называются — вопрос к
своему роутеру, а не к этому документу: цикл, печатающий состав целиком, есть в 3.8.2.

`sed` в этой команде подменяет значения секретных опций на `<есть>` — тот же маркер, каким obfmesh
показывает наличие токена в своих отчётах. Замаскированный вывод не страшно вставить в переписку;
без маски туда уедет URL подписки с токеном в пути.

**sing-box.** То же самое полем исходящего:

```json
{
  "outbounds": [
    {"type": "direct", "tag": "via-owg1", "bind_interface": "owg1"},
    {"type": "direct", "tag": "via-owg2", "bind_interface": "owg2"}
  ]
}
```

**Что угодно ещё** — `SO_BINDTODEVICE` (`curl --interface owg1`) либо привязка к адресу
`10.77.{i}.2`: правило `from 10.77.{i}.2 lookup 51820+i` уведёт такой сокет в нужный луч.

Правила раскладки:

1. Один сервис — один луч. Размазывать один сервис по двум лучам смысла нет: его поток всё равно
   упрётся в 180 Мбит/с в том луче, куда попал.
2. Делить по нагрузке, а не по алфавиту: тяжёлое (видео, загрузки) — на один луч, разговорчивое и
   мелкое (мессенджеры, API) — на другой.
3. Число лучей и раскладка связаны вручную. Уменьшили число лучей на сервере — секции, привязанные
   к исчезнувшему интерфейсу, останутся без выхода, пока их не перевесят.
4. Если следом ставится балансировщик (3.8), эта раскладка временная: все секции, привязанные к
   интерфейсу **базового** луча, переезжают на его адрес одним заходом. Пока хоть одна осталась на
   `bind_interface` базового луча, `obfmesh-balance on` не включится, а если бы включился — часть её
   трафика умирала бы в страховке.

**Проверка:**

```sh
wg show owg1 transfer; wg show owg2 transfer     # растут оба, когда работают обе группы
curl -s https://ifconfig.me; echo                # с клиента домашней сети
obfmesh status
```

**Откат.** Вернуть потребителям прежний выходной интерфейс и перезапустить их. Раскладка обратима
целиком: obfmesh при этом не трогается.

### 3.8 Балансировка соединений по лучам (пакет obfmesh-balance)

Шаг необязательный, и ставится он отдельным пакетом. В 3.7 по лучам разводятся сервисы, здесь —
отдельные соединения: все балансируемые секции Forkop выходят с адреса одного луча, а каким лучом
пойдёт конкретное соединение, решает цепочка nft. Смысл в том, что тяжёлая секция перестаёт забивать свой
луч в одиночку, пока соседний простаивает.

Луч выбирается **не жребием, а счётчиком**: `numgen inc mod 100` выдаёт новому соединению
следующую позицию карты весов, а позиции разложены между лучами вперемешку — при 50/50 лучи строго
чередуются. Одно без другого не работает: счётчик по сплошным диапазонам `0-49` и `50-99` отдал бы
пятьдесят соединений подряд одному лучу и был бы хуже жребия, а чередующаяся карта со жребием — это
просто жребий. Вместе они дают долю, соблюдённую на любом отрезке. Оговорка одна, и она честная:
счётчик общий на роутер, поэтому фоновые соединения сдвигают фазу и ровное деление конкретной
восьмёрки потоков не гарантировано (3.8.4).

Как это работает изнутри — SPEC.md, раздел «Балансировка соединений по лучам». Здесь порядок
действий.

**Перед началом.** Лучи живые (3.5), obfmesh отработал хотя бы сутки, sing-box метит свои сокеты —
на FriendlyWrt с Forkop это значение по умолчанию:

```sh
jsonfilter -i /etc/sing-box/config.json -e '@.route.default_mark'
# 134217728 — это 0x08000000, оно же mark_base пакета
```

#### 3.8.1 Пакет

Ставится установщиком из исходников — тем же способом и по той же причине, что и obfmesh (3.1):
собранного `.apk` нет.

```sh
scp -r openwrt/obfmesh-balance root@192.168.2.1:/root/     # с рабочей машины, каталог целиком
```

```sh
sh /root/obfmesh-balance/install-nosdk.sh
obfmesh-balance version
obfmesh-balance status | grep daemon    # «работает (pid N)» или «не работает»
obfmesh-balance check                   # пакет ещё выключен и ничего не делает
```

Установщик перед работой проверяет три вещи: что `obfmesh` на месте, что его версия не ниже 1.3.0
(иначе отказывается ставить) и что `nft` принимает `numgen`. Существующий
`/etc/config/obfmesh-balance` он не трогает, сервис на время замены файлов останавливает — мягко,
поэтому таблица и правила в ядре замену переживают.

**Строке про перезапуск от установщика верить нельзя, верить надо строке `daemon`.** Установщик
поднимает сервис обратно, а `start` при `option enabled 0` штатно ничего не запускает и пишет об
этом в лог. На только что установленном выключенном пакете демона может не быть — это нормально,
его поднимет `/etc/init.d/obfmesh-balance start` после включения (3.8.3).

В базе apk этих файлов, как и у obfmesh, нет: `apk info -e obfmesh-balance` молчит, обновление —
повторный запуск установщика, а `apk del obfmesh-balance` ничего не удалит и, что важнее, **не
позовёт `prerm`** — снимать таблицу и правила из ядра придётся руками, `obfmesh-balance teardown`
(3.8.6 и 6.1).

Зависимости: `obfmesh`, `nftables`, `jsonfilter`, `ip-full` — и ни одного `kmod-*`, почему именно
так, написано в 1.2. При установке из исходников их никто не разрешает: наличие `nft`, `jsonfilter`
и `ip` проверяет сам установщик, а `ip-full` ставится отдельно, пакетом (3.1).

**obfmesh нужен от 1.3.0.** Более старый снимает правила `31000+i` на каждом своём `apply`, считая
их чужими в своих таблицах. Пакетный менеджер этого не проверит — указать нижнюю границу версии в
`DEPENDS` нечем, — поэтому проверяют `postinst` (предупреждением) и `install-nosdk.sh` (отказом):

```sh
obfmesh version | head -1        # obfmesh 1.3.0 или новее
```

Пакет приходит выключенным (`option enabled 0`) и в этом состоянии не создаёт ни одной строки в
nft и ни одного `ip rule`. Знаком `?` со словами «что при выключенном пакете и ожидается» `check`
помечает ровно три вещи, и только их: отсутствие таблицы, отсутствие правила луча и отсутствие его
страховки. Всё остальное считается проблемой независимо от того, включён пакет или нет:

- `nft` не принимает `numgen` — карта весов не соберётся ни при каких настройках;
- состав лучей неизвестен: `obfmesh status --json` не отвечает либо `base_spoke` нет среди его
  лучей (эта строка обрывает проверку — дальше смотреть не на чем);
- ни одного живого луча;
- в конфигурации sing-box нет `route.default_mark` или он не совпадает с `mark_base`;
- секция Forkop сидит на `routing_mark`;
- секция Forkop привязана через `bind_interface` к интерфейсу **базового** луча — перевод не
  доведён, и пока он не доведён, балансировщик не включится (3.8.2);
- секция Forkop привязана через `bind_interface` к интерфейсу другого луча — она не балансируется,
  но и не ломается: включению это не мешает;
- таблица всё-таки есть, а карта весов в ней пуста — весь трафик идёт базовым лучом.

Последняя строка на свежей установке не появляется: таблицы ещё нет. Она для случая, когда пакет
выключили, а таблица осталась мягкой остановкой, и кто-то опустошил карту руками.

#### 3.8.2 Секции Forkop переезжают на адрес базового луча

**На адрес базового луча переводятся все секции, привязанные к лучам, — и к базовому, и к любому
другому, — одним заходом и до включения балансировки.** Это и есть полная выкатка: после неё луч
секции назначает балансировщик, а не человек. Жёсткая привязка секции к лучу и балансировка —
взаимоисключающие вещи: секция либо в раскладке, либо прибита к своему лучу, третьего состояния
нет.

Разница между двумя случаями — в срочности, а не в том, переводить или не переводить.

- Секция на интерфейсе **базового** луча ломается прямо сейчас и блокирует включение (3.8.3). Её
  перевод обязателен.
- Секция на интерфейсе **другого** луча не ломается и включению не мешает, но, оставшись, остаётся
  вне раскладки навсегда: её доля полосы балансировщику не достанется. Хуже того, регулятор её и
  не заметит: веса он держит ровными, а при `adapt = conns` считает только соединения со своей
  меткой — у непереведённой секции метки нет. Выйдет луч, забитый чужой секцией и получающий при
  этом ровно столько же новых соединений, сколько свободный.

Почему одним заходом и почему до включения:

- **Обрыв соединений один.** Правки применяет `uci commit forkop`, он же взводит перезапуск Forkop
  через procd-триггер, а перезапуск рвёт все соединения через прокси. Перевод в два подхода — это
  два обрыва вместо одного (порядок и проверка — ниже в этом разделе).
- **Промежуточное состояние теряет трафик.** Пока хоть одна секция сидит на интерфейсе базового
  луча, часть её соединений умирает в страховке. Балансировщик при ней включаться отказывается
  (3.8.3), но защита стоит на нём, а не на Forkop: перевести остальные секции она не поможет.
- **Раскладку считают по лучам целиком.** Ровные доли делят между лучами только те соединения,
  которые прошли через цепочку. Пока часть трафика прибита к лучу жёстко, половина полосы этого
  луча занята мимо раскладки, и замер «до и после» на половинчатом переводе ничего не показывает.
- **После включения перевод стоит дороже.** Соединения уже разложены по лучам, и рестарт Forkop
  оборвёт их все разом — вместе с теми, что и так работали как надо.

`bind_interface` для балансировки не годится: устройство выбирается до `connect()`, а цепочка живёт
в `hook output`, то есть уже после. Дальше всё зависит от того, на чей интерфейс указывает привязка.

- **Интерфейс базового луча (`bind_interface owg1`) — ломается.** Адрес источника у такой секции тот
  же, что у переведённой: `10.77.1.2` — единственный адрес на `owg1`. Под фильтр цепочки она
  попадает наравне с переведёнными, и её соединения тоже раскладываются по лучам. Но
  `SO_BINDTODEVICE`, который sing-box держит на сокете, переживает перемаршрутизацию: маршрут в
  таблице чужого луча ищется с ограничением на `owg1`, там его нет — и пакет уходит в страховку
  `31500+i`, то есть отбрасывается. Утечки не происходит, трафик просто умирает. Симптом при этом
  маскируется: sing-box переоткрывает оборванное соединение, и снаружи это редкие подвисания, а не
  отказ. Найдено на пилоте, подробности — CHANGELOG.md, запись obfmesh-balance 1.1.0.
- **Интерфейс другого луча (`bind_interface owg2` при базовом `owg1`) — не ломается.** Адрес
  источника другой, под фильтр цепочки секция не попадает вовсе: она не балансируется, ходит своим
  лучом и ничего не теряет. «Не ломается» — не значит «можно оставить»: переводится она тем же
  заходом, что и остальные. Оставляют такую привязку только сознательно и ровно ради одного —
  чтобы сервис навсегда сидел на своём луче, вне раскладки.

`routing_mark` не годится тоже, и это проверено на живом роутере: метка на сокет не ложится,
трафик уходит к провайдеру мимо туннеля (SPEC.md, «Что проверено и отвергнуто», п. 5).

Работает `inet4_bind_address`: сокет уходит с адреса базового луча и попадает под штатное правило
obfmesh `from 10.77.1.2 lookup 51821`, а дальше его подхватывает балансировщик.

Снимок до правки и состав секций — свой, с этой машины и на сегодня. Список секций живёт в Forkop и
меняется без нас, поэтому переписывать его в документ бессмысленно: смотрим то, что есть сейчас.

```sh
SNAP=/root/forkop.bak.$(date +%Y%m%d-%H%M%S)
( umask 077; cp /etc/config/forkop "$SNAP" )
chmod 600 "$SNAP"
md5sum /etc/config/forkop "$SNAP"          # хеши совпали — снимок полный
echo "$SNAP"

# в какой форме записан outbound_json у каждой секции: option или list
grep -nE 'outbound_json|^config ' /etc/config/forkop

# все включённые секции: подлежит ли переводу, имя, исходящий, привязка отдельной опцией
for s in $(uci -q show forkop | sed -n 's/^forkop\.\([A-Za-z0-9_]\{1,\}\)=section$/\1/p'); do
	[ "$(uci -q get "forkop.$s.enabled" 2>/dev/null)" = 0 ] && continue
	oj="$(uci -q get "forkop.$s.outbound_json" 2>/dev/null)"
	bi="$(uci -q get "forkop.$s.bind_interface" 2>/dev/null)"
	case "$oj" in *'}'*'{'*)
		printf 'НЕ ТРОГАТЬ\t%s\tв outbound_json больше одного объекта — разбирать руками\n' "$s"
		continue ;;
	esac
	if [ -n "$oj" ]; then mark='перевод'
	elif [ -n "$bi" ]; then mark='руками'
	else mark='пропуск'
	fi
	printf '%s\t%s\t%s\tbind_interface=%s\n' "$mark" "$s" "$oj" "$bi"
done
```

Снимок здесь — та же копия файла и тот же порядок, что в 3.7, включая правило снимать её
непосредственно перед работой. Файл с секретом: `subscription_urls` с токеном в пути, права `600`,
живёт только в `/root` на роутере. В `/www`, на рабочую машину и в переписку он не уезжает.

`grep` по самому файлу стоит рядом не для красоты. `uci show` и `uci -q get` не различают `option
outbound_json` и `list outbound_json` с одним элементом, а дальше эта разница решает, можно ли
трогать секцию циклом.

Цикл перебирает секции ровно так же, как это делает `check`: выключенные (`enabled 0`) пропускает,
остальные показывает целиком. Привязка бывает в двух местах — внутри `outbound_json` и отдельной
опцией секции, — и различать их надо: одного `uci show forkop | grep bind_interface` мало, он
находит обе формы вперемешку, а снимать их нужно по-разному. Опция секции снимается тем же заходом,
до `commit`: `check` считает секцию с `bind_interface` небалансируемой и говорит об этом отдельной
строкой.

Первая колонка отвечает на вопрос, подлежит ли секция переводу, и это главное, что из вывода надо
вынести. Включённая секция не значит переводимая:

- `перевод` — у секции есть `outbound_json`, то есть луч выбирается внутри него. Такую разбирает
  второй цикл; уже переведённые он пропустит сам.
- `пропуск` — `outbound_json` у секции нет вовсе и `bind_interface` отдельной опцией тоже нет: ни
  к какому лучу она не привязана, переводить в ней нечего. Так выглядит секция, работающая по
  подписке: узел ей приходит извне, а не из нашей конфигурации.
- `руками` — `outbound_json` нет, но стоит опция `bind_interface`: к лучу секция привязана, а
  шаблона исходящего, из которого можно взять тег, у неё нет. Второй цикл такую не трогает и
  называет отдельной строкой.
- `НЕ ТРОГАТЬ` — в `outbound_json` секции больше одного объекта, то есть это `list` из нескольких
  элементов. Автоматом такую секцию не перевести, разбор — ниже.

**Безымянные секции цикл не видит.** `sed` отбирает только именованные (`forkop.<имя>=section`), а
LuCI Forkop умеет заводить секции вида `forkop.@section[0]`. Есть такие или нет — отдельной
командой, и разбираются они руками:

```sh
uci show forkop | grep '=section$' | grep '@' || echo 'безымянных секций нет'
```

Когда пакет уже стоит, тот же состав, но с вердиктом по каждой секции, печатает

```sh
obfmesh-balance check | grep 'секция '     # балансируется / прибита к базовому лучу (это блокирует
                                           # включение) / прибита к другому лучу / мимо лучей вообще
```

Пример вывода первого цикла — боевой NanoPi R3S, снято 22.08.2026: семь включённых секций, из них
шесть с `outbound_json`. Это образец формата, а не чек-лист: на другой машине и в другой день
состав другой.

```
пропуск	proxysvc		bind_interface=
перевод	google	{"type":"direct","tag":"google-out","bind_interface":"owg2"}	bind_interface=
перевод	wg	{"type":"direct","tag":"wg-out","bind_interface":"owg2"}	bind_interface=
перевод	apple	{"type":"direct","tag":"apple-out","bind_interface":"owg1"}	bind_interface=
перевод	wgobf	{"type":"direct","tag":"wgobf-out","bind_interface":"owg1"}	bind_interface=
перевод	speedtest	{"type":"direct","tag":"speedtest-out","inet4_bind_address":"10.77.1.2"}	bind_interface=
перевод	kinopub	{"type":"direct","tag":"kinopub-out","bind_interface":"owg1"}	bind_interface=
```

Здесь же видно, зачем нужен живой вывод, а не список из документа. `proxysvc` — секция на подписке
VLESS: узлы ей приходят из `subscription_urls`, `outbound_json` у неё нет, ни к какому лучу она не
привязана, поэтому и стоит `пропуск`. Секция `kinopub` (домены `kino.pub`, `service-kp.com`) на
`owg1` — её в прежнем списке этого раздела не было вовсе, а переводить её надо наравне с
остальными. А `speedtest` уже сидит на `inet4_bind_address` базового луча: он переведён раньше, при
проверке `routing_mark` (SPEC.md, «Что проверено и отвергнуто», п. 5), и трогать его второй раз
нечего — второй цикл такую секцию пропустит сам, хотя в первом она помечена `перевод`.

Команды перевода под свой состав печатает второй цикл. Он ничего не меняет — только складывает
строки, которые останется прочитать и выполнить:

```sh
BASE=10.77.1.2                                   # адрес базового луча, см. base_spoke в 3.8.5
BASEIF=owg1                                      # его интерфейс: привязка к нему обязана уйти
for s in $(uci -q show forkop | sed -n 's/^forkop\.\([A-Za-z0-9_]\{1,\}\)=section$/\1/p'); do
	[ "$(uci -q get "forkop.$s.enabled" 2>/dev/null)" = 0 ] && continue
	oj="$(uci -q get "forkop.$s.outbound_json" 2>/dev/null)"
	bi="$(uci -q get "forkop.$s.bind_interface" 2>/dev/null)"
	case "$oj" in *'}'*'{'*)
		printf '# %s: в outbound_json больше одного объекта — НЕ ТРОГАТЬ, разбирать руками\n' "$s"
		continue ;;
	esac
	if [ -z "$oj" ]; then
		if [ "$bi" = "$BASEIF" ]; then
			printf '# %s: outbound_json нет, bind_interface=%s — интерфейс БАЗОВОГО луча, перевести обязательно и руками\n' "$s" "$bi"
		elif [ -n "$bi" ]; then
			printf '# %s: outbound_json нет, только bind_interface=%s — не базовый луч, включению не мешает\n' "$s" "$bi"
		else
			printf '# %s: ни outbound_json, ни bind_interface — к лучам не привязана, не трогаем\n' "$s"
		fi
		continue
	fi
	if [ "$(jsonfilter -s "$oj" -e '@.type' 2>/dev/null)" != direct ]; then
		printf '# %s: не type=direct — шаблон не подходит, руками\n' "$s"
		continue
	fi
	if [ "$(jsonfilter -s "$oj" -e '@.inet4_bind_address' 2>/dev/null)" = "$BASE" ] && [ -z "$bi" ]; then
		printf '# %s: уже на адресе базового луча — не трогать\n' "$s"
		continue
	fi
	printf 'uci set forkop.%s.outbound_json='\''{"type":"direct","tag":"%s","inet4_bind_address":"%s"}'\''\n' \
		"$s" "$(jsonfilter -s "$oj" -e '@.tag' 2>/dev/null)" "$BASE"
	if [ -n "$bi" ]; then
		printf 'uci -q delete forkop.%s.bind_interface\n' "$s"
	fi
done
```

**Строку `uci set` цикл печатает и для секции, сидящей на `bind_interface` другого луча, — это не
промах цикла, а замысел выкатки.** На этом роутере он выведет команды для `google` и `wg`, которые
стоят на `owg2`: жёсткие привязки снимаются со всех секций сразу, а дальше лучи назначает
балансировщик. Оставить какую-то секцию прибитой — решение, которое принимают отдельно: её строку
просто не выполняют и живут с тем, что этот сервис в раскладке не участвует, а его луч балансировщик
видит занятым.

Секцию, которая уже вышла на адрес базового луча, цикл пропускает со строкой-комментарием — на том
же роутере это `speedtest`, и повторно его никто не переписывает. Из прежнего JSON переносится
только `tag`; любое другое поле — скажем, `inet6_bind_address` — потеряется, поэтому вывод надо
сверить со снимком `/root/forkop.bak.<дата>`, прежде чем выполнять. Секции не с `type: direct`
цикл не трогает и называет их отдельно: шаблон исходящего у них другой.

**Секцию с несколькими `list outbound_json` цикл называет и не трогает.** Автоматом её не
перевести, и молчать тут нельзя — здесь три ловушки подряд:

- `uci -q get` на списке отдаёт все элементы одной строкой, склеенными через пробел. Отличить это
  от одного длинного JSON по самому значению нельзя, поэтому цикл ищет признак «после `}` дальше
  снова `{`»;
- `jsonfilter` на такой склейке разбирает **первый** объект, отдаёт его `tag` и возвращает `0`. Ни
  ошибки, ни пустого вывода — молчаливо неверный ответ;
- `uci set` на имя, за которым стоит список, схлопывает его в один `option`. Второй исходящий
  исчезает без единого сообщения, и заметить это можно только по снимку.

Сегодня на этом роутере строка `НЕ ТРОГАТЬ` не появится, но и запаса тут нет: пять секций
(`google`, `wg`, `apple`, `wgobf`, `kinopub`) хранят привязку именно как `list outbound_json` — с
одним-единственным элементом, — и только `speedtest` уже стал `option`. Второй элемент добавляется
из LuCI одним движением, и с этого момента цикл, не умеющий его заметить, стирал бы исходящий
молча. Разбирается такая секция руками — `uci delete forkop.<секция>.outbound_json`, затем
`uci add_list forkop.<секция>.outbound_json='...'` на каждый объект, — и результат сверяется со
снимком до `commit`.

**Перевод меняет форму записи: `list outbound_json` становится `option outbound_json`.** Это не
поломка и не побочный эффект, о котором надо жалеть: со `speedtest` так уже произошло при проверке
`routing_mark`, Forkop принял и работает. Знать об этом надо ради двух вещей. Во-первых, в `uci
show` разницы не видно, а в `/etc/config/forkop` видно — там и смотреть. Во-вторых, при откате
возвращается та форма, что записана в снимке, а не та, что стоит сейчас.

Секции без `outbound_json` он тоже только называет, и строки у них разные — ровно по колонке
первого цикла. `пропуск` (на этом роутере `proxysvc`) — «к лучам не привязана, не трогаем», и
делать с ней нечего. `руками` — луч у секции выбран, а шаблона исходящего, из которого взялся бы
тег, нет, и писать его придётся самому; строки тут две, и разница между ними важная. Привязка к
интерфейсу базового луча (`BASEIF`) обязательна к переводу — пока она есть, балансировщик не
включится. Привязка к другому лучу включению не мешает, но по замыслу выкатки переводится и она:
иначе секция навсегда останется вне раскладки. Цикл её только называет — тег и исходящий придётся
написать руками.

Дальше — выполнить отобранные строки и закрыть правку:

```sh
# ...сюда вставляются строки, напечатанные циклом и проверенные глазами...

md5sum /etc/sing-box/config.json     # хеш ДО
pidof sing-box                       # pid sing-box ДО

uci commit forkop
sleep 10
```

Теги (`"tag"`) сохраняются как были: на них ссылаются правила маршрутизации sing-box, и
переименование тега рвёт секцию вернее, чем неверная привязка.

**Перезапуск Forkop обязателен, но отдельной командой он, как правило, не нужен.** sing-box
перечитывает конфигурацию только при старте, поэтому без перезапуска новый `outbound_json` не
применяется вообще. Перезапуск при этом взводит сам `uci commit forkop`: в `/etc/init.d/forkop`
есть `procd_add_config_trigger`, commit шлёт procd событие `config.change`, а триггер стоит на
`reload` с задержкой 2 секунды — Forkop пересобирает `/etc/sing-box/config.json` и поднимает
sing-box заново. Безусловный `/etc/init.d/forkop restart` сразу за commit — это второй обрыв всех
соединений через прокси подряд: звонок кладётся дважды, загрузка начинается заново дважды.

Смотреть при этом надо на pid **sing-box**, а не Forkop: своего procd-инстанса у `forkop` нет,
и `ubus call service list '{"name":"forkop"}'` вернёт пустой объект в любом случае. Смены pid
sing-box Forkop дожидается сам, с таймаутом 15 секунд.

Поэтому рестарт делается **только по результату проверки**:

```sh
md5sum /etc/sing-box/config.json     # хеш обязан измениться
pidof sing-box                       # pid обязан стать другим

# ни одной строки: переведённые секции больше не привязаны к устройству
jsonfilter -i /etc/sing-box/config.json -e '@.outbounds[*].bind_interface'
# по строке 10.77.1.2 на каждую переведённую секцию
jsonfilter -i /etc/sing-box/config.json -e '@.outbounds[*].inet4_bind_address'
```

- Хеш другой, pid другой, `bind_interface` пуст (или остались только сознательно оставленные
  небазовые лучи), `inet4_bind_address` — адрес базового луча: перевод применён, руками ничего не
  делать.
- Хеш прежний или в `outbounds` всё ещё `"bind_interface":"owg1"` у переведённых секций — триггер
  не сработал либо собрал конфигурацию до правки. Только в этом случае:
  `/etc/init.d/forkop restart` и те же четыре команды ещё раз.
- Хеш другой, а pid прежний — конфигурацию пересобрали, но перечитать её некому. Тоже рестарт.

Смотреть надо именно `/etc/sing-box/config.json`: `uci show forkop` показывает и незафиксированные
правки, то есть скажет «переведено» ещё до `commit`, когда sing-box держит на сокете прежний
`SO_BINDTODEVICE`. Исполняется то, что лежит в этом файле.

Отсюда и порядок внутри блока: правки складываются все сразу, `commit` идёт последним и ровно один
раз. Опция, снятая после перезапуска, не подействует до следующего — а следующий стоит ещё одного
обрыва всех соединений через прокси. Делать в тихое время.

Что получилось — видно до включения балансировщика:

```sh
uci show forkop | sed -E "s/^([^=]*(urls?|token|key|secret|password|uuid))=.*/\1='<есть>'/" |
	grep bind_interface || echo 'привязок по интерфейсу не осталось'
# и то же самое по факту, а не по конфигурации Forkop: ожидается пустой вывод
jsonfilter -i /etc/sing-box/config.json -e '@.outbounds[*].bind_interface'
obfmesh-balance check                                         # ни одной строки ПЛОХО про секции
```

После полной выкатки `bind_interface` не остаётся ни у одной секции: ушли и обязательные (интерфейс
базового луча), и добровольные (интерфейс любого другого). Привязок к интерфейсу **базового** луча
не должно остаться в любом случае — на них и останавливается `obfmesh-balance on`. Привязка к
интерфейсу другого луча, оставленная сознательно, включению не мешает, и цена у неё названа выше:
сервис навсегда вне раскладки, а его луч балансировщик считает занятым.

Между перезапуском Forkop и включением балансировщика все переведённые секции идут одним лучом —
базовым. Это рабочее, но медленное состояние: клиентским путём один луч даёт около 111 Мбит/с
(3.8.4), поэтому два следующих шага делаются подряд.

#### 3.8.3 Включение

```sh
obfmesh-balance check          # ноль проблем — можно включать
obfmesh-balance on             # enabled = 1 и сразу применить
/etc/init.d/obfmesh-balance enable
/etc/init.d/obfmesh-balance start
obfmesh-balance status
```

`check` проходит по секциям Forkop и говорит про каждую, балансируется она или нет. `routing_mark`
и привязка к интерфейсу базового луча — проблема с объяснением, чем их заменить; привязка к
интерфейсу другого луча тоже названа, но включению не мешает.

**Включение при недоведённом переводе не состоится.** Если хоть одна секция Forkop привязана через
`bind_interface` к интерфейсу базового луча, `obfmesh-balance on` пакет не включает: печатает ту же
причину, что и `check`, — с именем секции, — и выходит. `enabled` остаётся `0`, ни таблицы, ни
правил не появляется, сеть работает как до команды. Это не придирка: такая секция под фильтр цепочки
попадает, а перемаршрутизацию не переживает, и часть её трафика умирает в страховке (3.8.2).

**Проверка стоит не на одном `on`.** Пока такая секция есть, ruleset не применяется ни одним путём:

- `obfmesh-balance on` — проверка идёт **до** записи в uci, поэтому `enabled` так и остаётся `0`:
  включённого пакета, который ничего не делает и переживает перезагрузку, не появляется;
- `obfmesh-balance apply` — отказывает и тут же сводит карту весов к базовому лучу. Таблица, SNAT,
  правила лучей и их страховки при этом остаются: по ним доживают соединения, уже переброшенные на
  небазовые лучи (3.8.6);
- `obfmesh-balance weights ...` — упирается в тот же отказ, потому что заканчивается вызовом
  `apply`. Веса и `mode = manual` в uci он к этому моменту уже записал: применены они не будут, но
  в конфигурации останутся;
- каждый такт демона, включая первый после старта. Секцию можно вернуть руками или пересозданием
  конфигурации Forkop уже на работающей балансировке — демон замечает это на ближайшем такте и
  уходит в мягкую остановку, записав причину в лог один раз, а не на каждом такте;
- `obfmesh-balance check` не отказ, а диагностика: он ничего не применяет, но считает такую секцию
  проблемой и называет её по имени.

Не проверяют ничего `status`, `off` и `teardown` — они и не раскладывают соединения.

Убрать причину можно двумя способами, и оба доводят перевод до конца:

- перевести названную секцию на `inet4_bind_address` базового луча вторым циклом из 3.8.2 — с
  `uci commit forkop` и проверкой оттуда же, что конфигурация sing-box перегенерировалась, — тогда
  она пойдёт в раскладку;
- если она должна остаться на своём луче, перевесить её на интерфейс **небазового** луча: под
  фильтр она не попадёт и ломаться будет нечему. Ценой того, что сервис останется на одном луче.

**Третий вариант есть, и отказ сам его печатает** — опция uci `allow_bind_interface`:

```sh
uci set obfmesh-balance.main.allow_bind_interface=1 && uci commit obfmesh-balance
```

Она снимает проверку разом во всех точках выше: `on`, `apply`, `weights` и такт демона перестают
смотреть на такие секции, и балансировка включается поверх них. Защиту снимает только явное
значение — `1`, `yes`, `true` или `on`; любое другое, включая опечатку и мусор, оставляет её на
месте, потому что ошибка в конфигурации не имеет права молча разрешить поломку трафика.

Чем платит тот, кто её включит. Секцию опция не чинит: соединения секций, привязанных через
`bind_interface` к интерфейсу базового луча, по-прежнему попадают под фильтр цепочки, и та доля из
них, которую карта весов уводит на чужой луч, отбрасывается страховкой `31500+i`. Приложение видит
не ошибку, а зависание с повтором; снаружи это редкие подвисания сервиса (3.8.2 и 5.2). Доля равна
суммарному весу небазовых лучей: при весе базового 90 гибнет каждое десятое новое соединение такой
секции, при ровной раскладке на два луча — каждое второе. Молча это не делается: демон пишет в лог
предупреждение, а `check` добавляет строку «защита снята вручную» и секцию проблемой считать не
перестаёт — нуля проблем при ней по-прежнему не получить.

Осмысленна она редко. Основной случай — когда точно известно, что названные секции трафика не
несут: секция в конфигурации Forkop осталась, а сервис за ней сейчас не используется (выключенную
секцию, `enabled 0`, защита и так не считает). Второй — короткое окно, в котором потеря сознательна:
замер или разбор цепочек на живом роутере, время, когда рестарт Forkop недопустим (он рвёт все
соединения через прокси и ждёт тихого часа), а балансировку надо поднять сейчас. Как постоянная
настройка опция означает ровно одно: часть трафика выброшена в blackhole навсегда. Вместо неё
переводят секцию (3.8.2).

#### 3.8.4 Проверка

Проверка состоит из четырёх частей, и порядок у них не случайный: сначала смотрим, что ruleset в
ядре, потом — что через него идёт трафик, потом меряем полосу, и в конце проверяем, что дефект
1.1.0 не воспроизводится.

**1. Что стоит в ядре.**

```sh
nft list table inet obfmesh_balance            # карта весов, цепочки out/pick/post
ip rule show | grep -E '^310[0-9][0-9]:'       # fwmark 0x200/0xff00 lookup 51822 у второго луча
ip rule show | grep -E '^315[0-9][0-9]:'       # его страховка: тот же селектор, blackhole
obfmesh-balance status                         # WEIGHT и KERNEL совпадают, daemon работает
obfmesh-balance logs -n 50
```

Колонка `WEIGHT` — что насчитал регулятор, `KERNEL` — что реально лежит в карте nft. Расхождение
значит, что демон не работает или последняя транзакция nft не прошла. В `mode = auto` при живых
лучах равной ёмкости `WEIGHT` обязан стоять на ровных долях (`50` и `50` на двух лучах) и не
двигаться: цель регулятора — константа, а не результат прошлого такта. Гуляющие веса при `adapt =
off` — это дефект, а не работа. Колонка `CONNS` заполнена только при `adapt = conns`, в остальных
случаях там прочерк; `DELTA` — прирост байт за такт, чистая диагностика: на решение он не влияет.

Чего эта часть **не** доказывает: что через цепочку прошёл хоть один пакет. Ruleset существует и
на роутере, где ни одна секция в него не попадает. `wg show owgN transfer` — «растут оба» — тоже
не доказательство: счётчики растут и от простой раскладки сервисов по лучам (3.7), без всякой
балансировки.

Правил на небазовый луч два, и второе важно не меньше первого:

```
31002:	from all fwmark 0x200/0xff00 lookup 51822
31502:	from all fwmark 0x200/0xff00 blackhole
```

Пока таблица `51822` жива, работает первое. Как только она опустела или правило `31002` кто-то
снял, помеченный пакет доходит до второго и отбрасывается — вместо того чтобы провалиться в
главную таблицу и уйти в WAN под MASQUERADE, к провайдеру открытым текстом. Проверено на роутере:
при живом `31002` команда `ip route get 1.1.1.1 mark 0x08000200` отвечает
`dev owg2 table 51822 src 10.77.2.2`, без него при живой страховке — `RTNETLINK answers: Invalid
argument`. Метка базового луча не задета: `ip route get 1.1.1.1 mark 0x08000000` по-прежнему даёт
`via 37.9.41.254 dev eth0`.

Если счётчики растут только у базового луча, а правил `31000+i` нет — смотрите, не снёс ли их
кто-то ещё: их держит демон, и он вернёт их на следующем такте. Если пропал не только правило, но и
страховка, помеченный трафик успел уйти мимо туннеля — `obfmesh-balance check` говорит об этом
отдельной строкой.

**2. Что трафик действительно проходит через цепочку.** Смотреть надо в conntrack, во время
нагрузки из пункта 3:

```sh
grep -c 'src=10.77.1.2' /proc/net/nf_conntrack     # соединения переведённых секций
grep -c 'mark=134218240' /proc/net/nf_conntrack    # из них уведённые на второй луч
grep 'mark=134218240' /proc/net/nf_conntrack | head -3
```

Метка луча `i` — это `mark_base | (i << 8)`, то есть `134217728 + 256*i`; для второго луча
`134218240` (`0x08000200`). У здоровой переброшенной записи прямое направление `src=10.77.1.2
dst=<цель>`, обратное — `dst=10.77.2.2`: это и есть SNAT, без него сервер отбросил бы пакет с чужим
адресом источника.

Ноль во второй строке при идущей нагрузке значит одно из двух: либо домен, который вы качаете, не
попадает ни в одну **переведённую** секцию (тогда его соединения до цепочки не доходят и меряется
что угодно, кроме балансировки), либо весь вес отдан базовому лучу — это видно в `obfmesh-balance
status`.

**3. Замер: стало ли лучше одному тяжёлому сервису.**

Замер делается **с клиента домашней сети**, а не с роутера, и это не придирка. `curl --interface
owgN` с роутера кладёт сокет прямо в луч: метки `0x08000000` на нём нет, потому что метку ставит
sing-box своим сокетам, а цепочка `out` отбирает по `meta mark 0x08000000 ip saddr 10.77.1.2`.
Такой замер честно меряет луч (3.5) и ничего не говорит о балансировке. Клиентский путь другой:
домен → FakeIP от sing-box → правило секции → исходящий с `inet4_bind_address` базового луча →
наша цепочка.

Требования к замеру:

- **Домен обязан попадать в переведённую секцию.** Проверяется не по названию секции, а пунктом 2:
  во время загрузки в conntrack появляются записи с `src=10.77.1.2`. Если их нет — возьмите домен,
  который эта секция ловит, либо добавьте тестовый домен в неё (ценой ещё одного `commit` и
  обрыва соединений, 3.8.2).
- **Соединений несколько.** Одно TCP-соединение по лучам не раскладывается — это свойство схемы, а
  не поломка: позиция карты выдаётся один раз на соединение, и потолок одной сессии на этой машине
  около 180 Мбит/с. Восемь параллельных загрузок — минимум, на котором видна разница.
- **Ровного деления 4/4 на восьми потоках может и не быть, и это не поломка.** Счётчик `numgen inc`
  один на весь роутер: фазу сдвигает любое постороннее соединение, прошедшее через цепочку между
  вашими. Наблюдалось на живом роутере: восемь потоков замера плюс два фоновых соединения легли
  **4/6**, а не 5/5. Ровными гарантированы доли позиций в карте, а не конкретный набор соединений,
  который вы открыли.
- **Источник файла не должен резать туннели.** `speed.cloudflare.com` не годится: с адресов VPS он
  отвечает 403. `cachefly` проверен и отдаёт полную полосу — но учтите, что сам по себе он **не
  входит ни в один список Forkop**, поэтому по умолчанию получает настоящий адрес и уходит мимо
  FakeIP. Перед замером обязательно `nslookup cachefly.cachefly.net 127.0.0.42`: адрес должен быть
  из `198.18.0.0/15`. Если нет — домен добавляется в переводимую секцию (см. выше) и убирается
  после замера.

```sh
# на клиенте домашней сети (Linux/macOS; на Windows — WSL или Git Bash)
URL=https://cachefly.cachefly.net/100mb.test
: > /tmp/omb-speed
for i in 1 2 3 4 5 6 7 8; do
	curl -s -o /dev/null -w '%{speed_download}\n' "$URL" >>/tmp/omb-speed &
done
wait
awk '{ s += $1 } END { printf "итого %.0f Мбит/с\n", s * 8 / 1000000 }' /tmp/omb-speed
```

Порядок: прогон А при `obfmesh-balance off`, прогон Б при `obfmesh-balance on`, подряд, тот же файл
и то же число потоков. `off` не трогает секции Forkop и не рвёт соединений — новые просто идут
базовым лучом, то есть ровно как без пакета (3.8.6).

Порядок величин на этой машине: **111 Мбит/с** одним базовым лучом и **359–390 Мбит/с** с
балансировкой, а после настройки маски RPS (3.6) — **411 Мбит/с**. Это ориентир, а не норматив:
смысл имеет разница А и Б, снятая подряд на своём роутере и своём канале. Вопрос, на который
отвечает замер, — «стало ли лучше, когда работает один тяжёлый сервис», а не «растут ли оба
счётчика `wg show`».

**Тот же замер, сделанный с роутера, занижает вдвое** — снято в одну сессию: 157 Мбит/с с роутера
против 348 с клиента. Причина не в схеме, а в процессоре: `curl` на роутере отбирает такты у
sing-box и обфускаторов, которые тот же трафик и обслуживают. Поэтому цифра с роутера в отчёт не
идёт вообще.

С числами из 3.5 эти не сравниваются, и путать их не надо: там сокет уходит в луч напрямую с
роутера, здесь трафик проходит клиентский путь целиком — перехват, sing-box, обфускатор, — и
процессор роутера тратится на всё это. Отсюда 111 Мбит/с одним лучом там, где голый сокет в тот же
луч даёт около 180.

Прогон Б без прироста при непустой второй строке из пункта 2 — это не «балансировка не работает», а
повод смотреть на CPU: два луча дают свои цифры только с настройкой роутера (3.6), без неё потолок
около 210 Мбит/с независимо от раскладки.

**4. Регрессия на дефект 1.1.0.** Дефект: секция, оставленная на `bind_interface` интерфейса
базового луча, попадает под фильтр цепочки, но `SO_BINDTODEVICE` на её сокете переживает
перемаршрутизацию — часть её соединений уходит на чужой луч и умирает в страховке `31500+i`. В
conntrack это выглядит как записи с меткой небазового луча, у которых `packets=0` **в обе стороны**
и нет SNAT (CHANGELOG.md, запись obfmesh-balance 1.1.0). Снаружи — редкие подвисания сервиса, и
больше ничего.

```sh
# ожидается 0: ни одного мёртвого соединения на небазовом луче
grep 'mark=134218240' /proc/net/nf_conntrack | grep -c 'packets=0 .*packets=0 '
```

Если полей `packets=` в файле нет вовсе, учёт conntrack выключен и проверка ничего не покажет:
`sysctl net.netfilter.nf_conntrack_acct`.

Ненулевой результат означает ровно две возможные причины: секция на `bind_interface` базового луча
(её назовёт `obfmesh-balance check`) или снятая руками защита `allow_bind_interface` (её `check`
называет отдельной строкой). Штатным путём в это состояние с 1.1.0 не попасть: при такой секции
`obfmesh-balance on` пакет не включает — печатает имя секции, оставляет `enabled = 0` и не создаёт
ни таблицы, ни правил (3.8.3). Проверять это на живом роутере, возвращая секцию на
`bind_interface`, стоит одного `commit` и обрыва всех соединений через прокси; без этой цены то же
самое проверяют регрессионные тесты набора `openwrt/obfmesh-balance/tests/run.sh` — они гоняются на
рабочей машине и падают на коде без защиты.

#### 3.8.5 Настройки

Полный список — приложение Г. Четыре, которые трогают чаще остальных:

```sh
uci set obfmesh-balance.main.base_spoke='1'    # адрес этого луча стоит в секциях Forkop
uci set obfmesh-balance.main.mode='auto'       # auto | manual | off
uci set obfmesh-balance.main.adapt='off'       # off | conns — только при mode = auto
uci set obfmesh-balance.main.interval='5'      # период такта регулятора, секунды
uci commit obfmesh-balance
/etc/init.d/obfmesh-balance reload
```

**`mode = auto` держит РОВНЫЕ доли**, `100/N` на каждый живой луч, и это же значение по умолчанию.
Прирост байт на решение не влияет — при равных лучах он мерит обслуженный спрос, а не ёмкость, и
вес всё равно управляет только новыми соединениями: открытые прибиты к лучу через `ct mark` и
переехать не могут (SPEC.md, «Веса»).

`adapt` включает поправку поверх ровных долей и действует только при `mode = auto`:

- `off` — по умолчанию. Для двух лучей равной ёмкости это же и оптимум, трогать нечего.
- `conns` — новые соединения уходят туда, где активных соединений сейчас меньше. Счёт снимается с
  `/proc/net/nf_conntrack` по метке соединения, поэтому за такт добавляется форк и проход по всей
  таблице: 20 мс при 141 записи, дальше около 0.08 мс на запись. Под торрентами это сотни
  миллисекунд на процессоре, который и так узкое место. Смысл включать есть тогда, когда соединения
  заметно разной длительности и стопка на лучах расходится сама.

Мёртвая зона у поправки — два сигма, и это видно в логе дословно: расклад 14/6 при двадцати
соединениях регулятор не трогает, а 20/0 правит.

Ручные веса — одной командой, она же переводит `mode` в `manual`:

```sh
obfmesh-balance weights 1:70 2:30
```

Ручная раскладка ложится в карту так же вперемешку, как ровная: при 70/30 в любом окне из десяти
позиций ровно три достаются второму лучу, а не «тридцать подряд в конце».

**Смена `base_spoke` — это и правка секций Forkop.** Адрес в `inet4_bind_address` обязан быть
адресом базового луча, иначе цепочка `out` не поймает ни одного пакета: секции продолжат ходить
своим лучом, а балансировка тихо перестанет работать.

Меняется при этом и то, какая привязка опасна. Секция на `bind_interface` луча, который стал
базовым, из безвредной превращается в ломающуюся: адрес источника у неё теперь совпадает с фильтром
цепочки. Переводить такие секции надо тем же заходом, что и менять `base_spoke`; `check` их назовёт,
а `on` при них не включится.

#### 3.8.6 Откат

Балансировка снимается в двух режимах, и выбор между ними — это выбор, дать ли дожить соединениям,
уже переброшенным на небазовые лучи.

**Мягко, без потерь.** Карта весов переводится на базовый луч целиком (`0-99 : mark_base`),
таблица, правила лучей и их страховки остаются в ядре. Новые соединения идут базовым лучом, то
есть ровно как без пакета; уже переброшенные доживают своим лучом — метка каждого пакета
восстанавливается из `ct mark`, SNAT на месте. Это режим штатной остановки: так работают
`obfmesh-balance off`, `mode = off`, `enabled = 0` и `/etc/init.d/obfmesh-balance stop`.

**Жёстко.** Таблица, правила `31000+i` и страховки `31500+i` уходят целиком. Соединение, уже
переброшенное на небазовый луч, после этого не рвётся, а **виснет** — и знать это надо заранее,
иначе симптом не опознать. Запись conntrack переживает снятие таблицы, `nf_nat` доигрывает
сохранённое отображение `10.77.1.2` → `10.77.2.2` уже без нашего SNAT, а восстанавливать метку из
`ct mark` больше некому: цепочек нет. Пакет уходит штатным путём базового луча, в `owg1`, но с
источником `10.77.2.2` — у пира `swg1` в AllowedIPs ровно `10.77.1.2/32`, и он молча его дропает.
Ни ответа, ни отказа: соединение стоит до таймаута приложения. Режим нужен, когда след пакета надо
убрать из ядра прямо сейчас; молча он не выполняется, команда предупреждает.

Мягкое — не «аккуратный вариант жёсткого», а единственный способ уйти без потерь: привязка
соединения к лучу живёт в `ct mark` и в записи SNAT внутри conntrack, а сбросить их нечем —
утилиты `conntrack` на этом роутере нет (`which conntrack` — пусто).

**Мягкое состояние переходит в жёсткое само, но только пока работает демон.** Выключенный пакет не
имеет права держать своё на роутере вечно: пролежав `OMB_SOFT_GRACE` секунд (по умолчанию 600 —
типичный idle-таймаут прикладного соединения) при `enabled = 0` или `mode = off`, демон снимает
таблицу, правила и страховки сам. Отсчёт идёт в его цикле, поэтому убитый демон отсчитывать нечем:
после `stop` мягкое состояние живёт до перезагрузки или до явного `teardown`. Порядок отката из
этого и следует.

Штатно — дать переброшенным соединениям дожить:

```sh
obfmesh-balance off                            # мягко; Forkop не трогать
                                               # демон НЕ останавливать: он и снимет остальное
sleep 660                                      # OMB_SOFT_GRACE 600 с плюс такт демона
ip rule show | grep -E '^31[05][0-9][0-9]:' || echo 'правил и страховок нет'
nft list table inet obfmesh_balance 2>/dev/null || echo 'таблицы нет'
/etc/init.d/obfmesh-balance stop
/etc/init.d/obfmesh-balance disable
```

`obfmesh-balance status` на этом месте отвечает только за половину вопроса: про таблицу он скажет
(строка `nft table` — «есть» или «НЕТ»), а в строке `ip rule` печатает формулу приоритетов из
конфигурации, а не то, что стоит в ядре. Что осталось в ядре, показывает `ip rule show`, и ничто
другое.

Сразу, когда след надо убрать немедленно и ждать нечего:

```sh
obfmesh-balance off                            # чтобы демон не собрал ruleset заново
obfmesh-balance teardown                       # таблица, правила 31000+i и страховки 31500+i
/etc/init.d/obfmesh-balance stop
/etc/init.d/obfmesh-balance disable
ip rule show | grep -E '^31[05][0-9][0-9]:' || echo 'правил и страховок нет'
nft list table inet obfmesh_balance 2>/dev/null || echo 'таблицы нет'
```

Порядок `off` → `stop` без ожидания и без `teardown` оставляет в ядре таблицу, правила и
страховки: `stop` мягкий, а снимать их станет уже некому.

Дальше, если нужно вернуть жёсткую раскладку из 3.7, — вернуть секциям прежнюю привязку по снимку
`/root/forkop.bak.<дата>`, порядком восстановления из 3.7. Возвращается та форма, что записана в
снимке: `option` или `list outbound_json` целиком, если `bind_interface` стоял внутри, и отдельная
опция секции, если она была отдельной. Порядок именно такой: сначала снять балансировку, потом
менять привязки.

Снять пакет целиком — файлами, потому что в базе apk его нет (3.8.1):

```sh
obfmesh-balance off; obfmesh-balance teardown     # сначала убрать своё из ядра
/etc/init.d/obfmesh-balance stop; /etc/init.d/obfmesh-balance disable
rm -f /etc/init.d/obfmesh-balance /usr/bin/obfmesh-balance /etc/config/obfmesh-balance
rm -rf /usr/lib/obfmesh-balance /var/run/obfmesh-balance
```

`teardown` здесь обязателен и стоит первым: при установке пакетом его позвал бы `prerm`, а тут
звать некому. Ниже — что сделал бы пакетный менеджер, если пакет когда-нибудь будет собран.

Свой след из ядра убирает `prerm`: он зовёт `obfmesh-balance teardown`, то есть жёсткое снятие со
всеми его последствиями для переброшенных соединений. Не `stop` — остановка сервиса мягкая и
таблицу с правилами оставляет. Рабочий каталог `/var/run/obfmesh-balance` уносит `postrm`. При
**обновлении** пакета (`PKG_UPGRADE=1`) оба шага пропускаются: таблица, правила и страховки
остаются на месте, новый демон пересоберёт своё на ближайшем такте. Секции Forkop остаются на
адресе базового луча — их возвращает только человек.

Обновление установщиком устроено так же и по тому же замыслу: он останавливает сервис мягко,
заменяет файлы и поднимает обратно, а таблица, правила и страховки замену переживают —
переброшенные соединения её не замечают.

### 3.9 Реактивный обход недоступных сайтов (пакет obfmesh-onwatch)

Шаг необязательный, и это третий пакет проекта. В 3.7 по лучам разводятся сервисы, в 3.8 —
отдельные соединения, здесь — **отдельные сайты, которые провайдер обрывает прямо сейчас**. Детектор замечает
обрыв, проверяет цель через луч и заворачивает её туда, **ничего не перезапуская**: заворачивание —
это одна транзакция `nft add element` в живое множество. Ни dnsmasq, ни sing-box, ни Forkop, ни
firewall пакет не трогает, открытые соединения не рвёт, `uci commit` при заворачивании не делает ни
разу. Требование было поставлено дословно так: «ронять нельзя интернет на постоянной основе,
необходимо придумать реактивную альтернативу».

**Что именно ловится.** Не то, что кажется. На этом провайдере блокировка выглядит не как молчание
сервера: из 16 проверенных доменов заблокированы 6, и у всех шести TCP-рукопожатие проходит
успешно — в conntrack они `ESTABLISHED`, а RST прилетает уже на ClientHello (`curl` rc=35).
Детектор «висит в `SYN_SENT`» прошёл бы мимо всех шести. Инжектированный RST узнаётся по отпечатку
**`tcp window 32`** — снят с 45 живых RST, ложных ноль. Настоящее молчание тоже бывает, но реже: из
тех же 16 доменов так вёл себя один, и на него есть второй сенсор с порогом повыше.

**Откуда берутся имена.** Пассивным захватом ответов DNS на петле: dnsmasq на этой прошивке собран
с `cache-size=0` и единственным апстримом `127.0.0.42`, поэтому все запросы клиентов физически
летят по `lo` открытым текстом. Цена замерена: `lo` несёт 7 пакетов/с против 72000/с на `eth0` под
нагрузкой, то есть **0.003 % ядра**; на `br-lan` или `eth0` тот же захват стоил бы до 16 % одного
ядра, а потолок 430 Мбит/с на этом роутере задан именно процессором. Задержка отрицательная: имя
известно раньше, чем клиент отправит SYN.

**Прежде чем завернуть — проба.** Цель проверяется дважды, напрямую и через луч, по настоящему
адресу через `--resolve`. Заворачивается только то, что прямо не открывается, а через луч
открывается. Не помогло и через луч — цель уходит в карантин: сайт лежит целиком, а не блокируется.

**Это временная мера, а не замена Forkop.** Доменных правил sing-box пакет не умеет и FakeIP не
выдаёт: он работает по парам адрес.порт. Накопленное переносится в секцию `on_test` Forkop
руками — 3.9.6, и это единственная операция во всей фиче, которая рвёт связь всему дому.

Своё и только своё, как у соседей: таблица `inet obfmesh_onwatch`, полоса `ip rule` `30000+i`
(правила лучей), `30500+i` (их страховки) и `30511` (общая страховка), база метки `0x02000000` с
номером луча в младшем байте. Два процесса procd: захват DNS и решатель, `START=97` и `STOP=8` —
строго после соседей на старте и строго раньше них на остановке.

**Чего пакет не может.** Список закрытый, и это не список планов:

1. **Различить устройства.** За двойным NAT весь дом — один адрес `192.168.2.151`, а на `lo`
   теряется и он: источник всегда `127.0.0.1`.
2. **Узнать домен при заходе по голому IP без DNS** — торренты, игры, вшитые в приложение адреса.
3. **Увидеть запрос по DoH или DoT.** Он идёт по 443 или 853 и неотличим от обычного трафика. Это
   граница метода, а не сбой: имени у такой цели не будет никогда, и при `require_domain = 1` она
   не завернётся. Лечится только `obfmesh-onwatch pin <имя>`.
4. **Отличить блокировку от 403 или 451 полностью рабочего сайта** (`grok.com`, `chatgpt.com`,
   `notion.so`): их проба видит как «сайт работает» и не заворачивает.
5. **Отличить блокировку по SNI от блокировки по IP.** Выражения SNI в nftables 1.1.6 нет.
6. **Порвать уже открытые соединения.** Утилиты `conntrack` в прошивке нет; заворот действует на
   новые, человек видит результат после F5.
7. **Поймать деградацию** — медленно, рвётся на середине. Это не отказ, сигнала нет.
8. **QUIC и IPv6.** Детектора для UDP/443 нет, глобального IPv6 на роутере нет вовсе. Оговорка:
   уже завёрнутую пару адрес.порт цепочка уводит в луч и по UDP — но опознать её по QUIC она не
   может.
9. **Заменить Forkop** — см. выше.

#### 3.9.1 Пакет

Ставится установщиком из исходников, тем же способом и по той же причине, что obfmesh (3.1) и
балансировщик (3.8.1): собранного `.apk` нет, SDK OpenWrt бывает только под Linux x86_64
(PACKAGING.md, 2.1), и пакеты на этом роутере живут вне базы apk.

Зависимости — `obfmesh` (от 1.4.0), `nftables`, `jsonfilter`, `ip-full`, `tcpdump`, `curl`. Первые
четыре уже стоят с 3.1 и 3.8.1, `tcpdump` обычно нет:

```sh
apk add tcpdump           # единственный источник имён; без него при require_domain 1 не
                          # завернётся ничего, кроме закреплённого руками
tcpdump --version
```

Ни одного `kmod-nft-*` в зависимостях нет намеренно, по той же причине, что у балансировщика
(1.2): на этой прошивке ядро 6.1.141, а `kmod-*` в её feed собраны под 6.12.94. Нужное проверяется
не наличием пакета, а способностью **работающего** ядра принять выражение, пробами `nft -c -f -`.
Установщик делает две — `tcp window 32` и `numgen inc mod N map @spokes`, и обе предупреждением, а
не отказом; `obfmesh-onwatch check` повторяет их и добавляет третью, на динамическое множество со
счётчиком. Тексты советов у установщика и у `check` дословно одни и те же, и это сторожит тест.

```sh
scp -r openwrt/obfmesh-onwatch root@192.168.2.1:/root/     # с рабочей машины, каталог целиком
```

```sh
sh /root/obfmesh-onwatch/install-nosdk.sh
obfmesh-onwatch version
obfmesh-onwatch check
```

Установщик отказывается ставить **половину пакета**: если в исходниках нет хоть одного из восьми
файлов, он не кладёт ничего. Половина — это не «работает похуже», а два разных отказа: без `nft.sh`
детектор считает, но не заворачивает, а без `rules.sh` метка луча остаётся без своего `ip rule` и
уходит в WAN открытым текстом. Существующий `/etc/config/obfmesh-onwatch` он не перезаписывает,
сервис на время замены файлов останавливает (`cp` поверх исполняющегося скрипта обрывает его
посреди файла с кодом возврата 0) и делает `enable`.

**Строке про запуск от установщика верить нельзя, верить надо строке `daemon` в `status`.** Пакет
приходит выключенным, а `start` при `option enabled 0` штатно ничего не поднимает и пишет об этом в
лог. То есть сразу после установки нет ни решателя, ни захвата DNS — и это нормально.

В базе apk этих файлов нет, как и у соседей: `apk info -e obfmesh-onwatch` молчит, обновление —
повторный запуск установщика, а `apk del obfmesh-onwatch` ничего не удалит и **не позовёт `prerm`**
— снимать таблицу и правила из ядра придётся руками (3.9.7). Страницы LuCI у пакета нет вовсе.

#### 3.9.2 Порядок первого запуска

Пять шагов, и один из них длится сутки. Спешить здесь не с чем: пока сухой ход не снят, пакет
ничего не заворачивает, а смотреть надо именно на его решения.

**Шаг 1. Поставлен и выключен.** Два выключателя, и снимаются они по очереди:

```sh
uci get obfmesh-onwatch.main.enabled       # 0 — нет ни таблицы nft, ни единого ip rule
uci get obfmesh-onwatch.main.dry_run       # 1 — детектор считает и пишет журнал, но не заворачивает
nft list table inet obfmesh_onwatch 2>/dev/null || echo 'таблицы нет, и это правильно'
ip rule show | grep -E '^30[05][0-9][0-9]:' || echo 'полоса ip rule пуста'
```

**Шаг 2. `obfmesh-onwatch check` — что помешает включить.** Знаком `?` со словами «что при
выключенном пакете и ожидается» помечается ровно то, чего при `enabled = 0` быть и не должно:
таблицы, правил лучей, страховок, работающего захвата и непустой карты имён. Проблемой считается
всё остальное независимо от того, включён пакет или нет:

- нет `tcpdump` — имён взять неоткуда; нет `curl` — нечем пробить цель через луч перед заворотом;
- `nft` не принимает `tcp window 32` — детектор слеп к главному классу блокировок;
- `nft` не принимает `numgen` — карта лучей не соберётся, ни одна транзакция не пройдёт;
- `nft` не принимает динамическое множество со счётчиком — копить улики в ядре нечем;
- `mark_base` задевает чужие биты, полоса `ip rule` занята чужим правилом;
- состав лучей неизвестен (`obfmesh status --json` молчит) или ни одного живого луча;
- `spoke_mode` — номер луча, которого среди живых нет.

Отдельной строкой `check` печатает то, чего пробой не проверить: **`nft -c` не ловит множество без
`flags dynamic`, в которое пишет правило**, — это скажет только ядро на настоящей транзакции. Если
`apply` прошёл, а множества улик пустые, смотреть надо сюда.

**Шаг 3. Включить, оставшись в сухом ходу.** `on` правит только свой uci-файл и ничего не
перезапускает; `dry_run` он не трогает.

```sh
obfmesh-onwatch on                       # enabled = 1: сначала правила и страховки, потом таблица
/etc/init.d/obfmesh-onwatch start        # захват DNS и решатель
obfmesh-onwatch status
```

Порядок внутри `on` обратный привычному и обязателен: **правила и страховки ставятся ДО таблицы**.
Между поднятой таблицей и поставленным правилом лежит окно, в котором помеченный пакет уходит в
главную таблицу и дальше в WAN открытым текстом — проверено, `ip route get 1.1.1.1 mark 0x02000001`
без правила отвечает `via 37.9.41.254 dev eth0`. При отсутствии живых лучей `on` откажется
применять таблицу и скажет об этом: метка без живого луча ловится страховкой `blackhole`, то есть
цель просто пропадает.

Что должно появиться:

```sh
nft list table inet obfmesh_onwatch | head -40   # множества never4, hot4, quarantine4,
                                                 # rst_hits, syn_watch, карта spokes, цепочки
ip rule show | grep -E '^300[0-9][0-9]:'         # 30001: fwmark 0x2000001/0x20000ff lookup 51821
ip rule show | grep -E '^305[0-9][0-9]:'         # 30501: тот же селектор, blackhole
ip rule show | grep -E '^30511:'                 # общая страховка на любой номер луча
obfmesh-onwatch status                           # daemon и «захват DNS» — работают
```

Страховок две ступени, и вторая не лишняя. Персональная (`30500+i`) уходит вместе с лучом,
покинувшим состав; общая (`30511`) стоит всё время работы пакета и ловит метку любого номера луча —
в том числе того, чьё правило снять успели, а соединение с его `ct mark` ещё живо.

Тут же — что захват действительно видит **имена**, а не только пакеты:

```sh
obfmesh-onwatch status | grep -E 'захват DNS|ответов DNS|карта имён'   # ответы растут,
                                                  # дубликаты тоже: петля отдаёт пакет дважды
wc -l /var/run/obfmesh-onwatch/domains.map
tail -3 /var/run/obfmesh-onwatch/domains.map      # строки вида '140.82.121.4 github.com 1787...'
```

Карта пуста после нескольких минут живого захвата — смотреть надо на сам вывод `tcpdump`:

```sh
tcpdump -i lo -nn -l -v -s 512 --immediate-mode -c 5 \
	'udp src port 53 and src host 127.0.0.42'
```

В строке обязано стоять **имя владельца записи**: `7387 1/0/1 github.com. A 140.82.121.4 (72)`.
Если там `1/0/1 A 140.82.121.4` без имени — сборка `tcpdump` не печатает владельца даже при `-v`, и
заворачивать по имени будет нечего. Ключи в этой строке подобраны не наугад: `-q` превращает вывод
в `UDP, length 283` и ломает разбор целиком, а `-vv` печатает ещё и секцию additional — glue-записи
оттуда привязались бы к спрошенному имени, и в карту попало бы враньё.

**Шаг 4. Сутки в сухом ходу.** Пакет считает, объясняет и пишет журнал решений, но не заворачивает
ничего. Смотреть — журнал:

```sh
obfmesh-onwatch list                     # завёрнуто (в сухом ходу пусто), карантин, закреплённое
obfmesh-onwatch logs -n 100
{ cat /etc/obfmesh-onwatch/journal.jsonl
  cat /var/run/obfmesh-onwatch/journal.pending; } 2>/dev/null | tail -20
```

Журнал лежит на overlay и переживает перезагрузку; последние записи ещё не уехали на флеш и лежат
рядом в tmpfs — поэтому читаются оба файла. Сам файл не растёт выше 128 КиБ, при обрезке в нём
остаются последние 500 записей, а на флеш они уезжают не чаще раза в минуту: NAND не заслужил
строки в минуту, помноженной на такт демона. Свой журнал здесь не для красоты — кольцевой лог
роутера тоже 128 КиБ (`logd -S 128`), и решения вынесли бы оттуда диагностику obfmesh и Forkop за
час.

События в журнале: `dry-run` — «завернул бы, и вот почему», `wrap` — завернул, `quarantine` — луч
не помог или цель и так работает, `probe-failed` — проба прошла, а `nft add element` отказал, `pin`
и `unpin` — руки человека, `limit` — упёрлись в `max_hot` или сработал предохранитель,
`journal-overflow` — сколько решений потеряно между записями на флеш.

```sh
grep '"event":"dry-run"' /etc/obfmesh-onwatch/journal.jsonl | tail -20
jsonfilter -i /var/run/obfmesh-onwatch/state.json -e '@.last_reason'
```

Последняя строка и есть **мера слепоты к DoH**: в ней сказано, сколько было кандидатов и у скольких
из них не нашлось имени. В `obfmesh-onwatch status` этой доли нет — там счётчики захвата и строка
«слепота к DoH», которая всегда `ДА`, потому что это свойство метода, а не состояние процесса. Ещё
раз в лог та же доля уходит строкой «имени нет у ~N % кандидатов».

Что искать в сутках наблюдения — один вопрос: похож ли список на правду. Знакомые домены — да.
Адрес CDN общего пользования, за которым стоят сотни чужих имён, — повод посмотреть, по какому имени
он опознан: завернув такой адрес, вы заворачиваете и всех остальных на нём. Рабочий сайт,
отвечающий 403 или 451, в «завернул бы» не попадёт вовсе — проба видит его как работающий, и в
журнале он окажется событием `quarantine` с причиной, где стоит `curl rc 0`.

**Шаг 5. Снять сухой ход.**

```sh
uci set obfmesh-onwatch.main.dry_run=0
uci commit obfmesh-onwatch
```

`uci commit obfmesh-onwatch` пишет только свой файл. Это **не** тот commit, который рвёт соединения
дому: перезапуск Forkop взводит `uci commit forkop`, а здесь ни триггера, ни рестарта нет. Демон
перечитывает конфигурацию сам — как только файл стал новее его отметки, а вслепую всё равно раз в
пять минут; `/etc/init.d/obfmesh-onwatch reload` нужен только чтобы поднять демона, если его нет.

Дальше — первый настоящий заворот:

```sh
obfmesh-onwatch logs -n 50 | grep -i 'завёрнуто'
obfmesh-onwatch list
nft list set inet obfmesh_onwatch hot4
```

Человеку, у которого сайт был оборван, надо обновить страницу: заворот действует на новые
соединения, а старое доигрывает по-прежнему — рвать его нечем.

#### 3.9.3 Команды

```sh
obfmesh-onwatch status [--json]   # лучи, завёрнутые цели, таблица, счётчики захвата DNS
obfmesh-onwatch check             # диагностика: инструменты, пробы nft, метки, полоса, лучи, захват
obfmesh-onwatch list              # что завёрнуто, что в карантине, что закреплено — с именами
obfmesh-onwatch pin <цель>        # завернуть насовсем: имя, адрес либо хост:порт
obfmesh-onwatch unpin <цель>      # снять закрепление и вывести цель из обхода
obfmesh-onwatch export            # закреплённое и завёрнутое готовыми командами pin
obfmesh-onwatch on | off          # включить или выключить пакет в uci
obfmesh-onwatch apply             # собрать таблицу, правила и страховки заново
obfmesh-onwatch teardown          # снять таблицу, правила и страховки целиком
obfmesh-onwatch logs [-f|-n N]    # свои строки из системного лога
obfmesh-onwatch version           # версии пакета, obfmesh, nftables, tcpdump, curl
```

Что стоит знать про каждую, кроме очевидного:

- **`pin`** принимает имя (`rutracker.org`), адрес (`104.21.0.1`) или пару (`example.com:80`); без
  порта берутся оба, за которыми следит детектор, — 80 и 443. Имя закрепляется **именем**: адреса
  под него подставит демон из карты захвата, в том числе те, которых ещё не видел. Закрепление
  живёт в `/etc/obfmesh-onwatch/pinned.list` и переживает перезагрузку. В сухом ходу оно
  записывается, но не применяется, и `pin` об этом говорит. Адрес из `never4` — своя сеть, сеть
  провайдера, резолвер — не принимается: завернув резолвер, роутер отрезал бы себя от имён.
- **`unpin`** снимает закрепление и убирает пару из `hot4`. Карантин он **не** трогает: цель,
  уехавшую в карантин, вынимают из множества руками (3.9.5).
- **`off`** — мягко: `hot4` пустеет, новые соединения идут напрямую, а таблица, правила и страховки
  остаются, потому что по ним доживают уже завёрнутые соединения (луч записан у них в `ct mark`).
  **В отличие от балансировщика, само это состояние не снимается никогда**: аналога `OMB_SOFT_GRACE`
  у onwatch нет, и мягкая остановка держится до `teardown` или до перезагрузки.
- **`teardown`** — жёстко и сразу. Завёрнутые соединения после него не рвутся, а **виснут**: запись
  conntrack переживает снятие таблицы, `nf_nat` доигрывает сохранённый SNAT уже без нашей цепочки,
  пакет уходит в WAN с адресом луча и до назначения не доедет. Ни ошибки, ни разрыва — приложение
  ждёт своего таймаута. Сбросить conntrack нечем.
- **`logs`** — это `logread -e obfmesh-onwatch`; журнал решений в системный лог не льётся.

Отдельной подкоманды для журнала нет: он читается файлами (3.9.2, шаг 4).

#### 3.9.4 Настройки и цена ошибки в каждой

Файл `/etc/config/obfmesh-onwatch`, секция `config onwatch 'main'`. Секретов в нём нет, но есть
настройка живого роутера, поэтому он объявлен в `conffiles` и установщик его не перезаписывает.
Демон перечитывает конфигурацию сам, `reload` нужен только чтобы поднять его, если он не работает.
Границы значений и поведение на негодном значении — приложение Д.

| Опция | По умолчанию | Что делает и чем платят за ошибку |
|---|---|---|
| `enabled` | `0` | Главный выключатель. При `0` нет ни таблицы, ни `ip rule`, ни журнала. **Цена включения без `check`:** детектор поднимет свою таблицу и начнёт метить соединения, а метка без своего правила — это не отказ, а утечка: `ip route get 1.1.1.1 mark 0x02000001` без правила отвечает `via 37.9.41.254 dev eth0`. Поэтому правила и страховки ставятся раньше таблицы |
| `dry_run` | `1` | Сухой ход: считает, объясняет, пишет журнал, но не заворачивает. **Цена снятия раньше времени:** ложно опознанная цель уезжает в луч автоматически и молча, а для человека это выглядит не как поломка, а как «сайт стал медленнее» — завёрнутый трафик идёт мимо flowtable (в `ft` лежат `br-lan`, `docker0`, `eth0`, но не `owg`). Мусор в опции оставляет сухой ход на месте: опечатка не имеет права начать заворачивать живой трафик |
| `interval` | `5` | Период такта решателя, секунды. **Цена малого:** чтение множеств — это форк `nft` на такт, а процессор роутера и так узкое место. **Цена большого:** ровно на столько задерживается заворот уже опознанной цели, и человек всё это время видит оборванную страницу |
| `spoke_mode` | `auto` | `auto` — завёрнутое раскладывается по живым лучам (`numgen inc mod N map @spokes`), лёгший луч выпадает из карты в тот же такт. Число — фиксированный луч. **Цена фиксированного:** он лёг — и все завёрнутые цели встали разом, не «пошли в обход», а именно встали: помеченный пакет без живой таблицы ловится страховкой `blackhole`. При `auto` такого не бывает: живых лучей не осталось — карта пуста, новые соединения идут напрямую |
| `require_domain` | `1` | Заворачивать только цели, у которых есть имя из захвата DNS. **Цена `0`:** в луч поедет всё, что подошло под отпечаток RST, включая соединения без DNS вовсе (NTP, STUN, игровые серверы); хуже то, что один адрес CDN несёт сотни чужих имён, и завернув его по одному имени, вы заворачиваете и всех остальных. **Цена `1`:** цель, чьё имя ушло мимо dnsmasq (DoH в браузере, кеш клиента), не завернётся — только руками, `pin` |
| `hot_ttl` | `2h` | Сколько завёрнутая цель остаётся завёрнутой без нового трафика; отсчёт ведёт ядро, любой пакет к цели его продлевает. **Цена большого:** сайт, который разблокировали месяц назад, ещё `hot_ttl` ходит через луч — медленнее и мимо flowtable. **Цена малого:** цель протухает между двумя визитами человека, и первый заход после паузы снова ловит RST |
| `quarantine_ttl` | `12h` | Сколько цель, которую не спас луч, не проверяется повторно. **Цена малого:** демон долбит лежащий сайт пробой, это форк `curl` на такт на цель. **Цена большого:** сайт починили через час, а карантин держит его ещё одиннадцать |
| `rst_threshold` | `2` | Сколько инжектированных RST на пару адрес.порт считать блокировкой. **Цена `1`:** один случайный RST с окном 32 — и цель уезжает в луч; отпечаток проверен, но одного пакета для автоматического решения мало. **Цена большого:** человеку придётся столько раз обновить страницу, прежде чем детектор сработает |
| `syn_threshold` | `3` | Сколько SYN без ответа считать чёрной дырой. Порог выше, чем у RST, намеренно: молчание сервера — это ещё и обычная авария на той стороне. **Цена малого:** любой временно лежащий сервер уезжает в луч, а оттуда в карантин на `quarantine_ttl`. **Цена большого:** настоящая чёрная дыра ловится с задержкой в несколько попыток |
| `probe_timeout` | `5` | Сколько ждать пробу, прежде чем считать её неудавшейся. **Цена малого:** живой, но медленный луч не успевает ответить, и цель уезжает в карантин, хотя луч бы её спас. **Цена большого:** такт растягивается на `probe_timeout` за каждую проверяемую цель |
| `max_hot` | `256` | Предел числа одновременно завёрнутых целей, он же размер множества в ядре. **Цена большого:** весь этот трафик идёт мимо flowtable и упирается в процессор роутера — тот самый, что задаёт потолок 430 Мбит/с. **Цена малого:** после исчерпания новые цели не заворачиваются; молча этого не происходит (строка в лог и запись `limit` в журнал), но сайт для человека всё равно останется оборванным |
| `mark_base` | `0x02000000` | База метки, номер луча в младшем байте. Трогать без нужды нечего, а цена ошибки посчитана числами: `0x04000000` — бит tproxy Forkop, его правило `pref 105` уводит такой пакет в `local default dev lo`, то есть в петлю на петлевом интерфейсе; ненулевой байт `0x0000FF00` — номер луча у obfmesh-balance, и наш пакет ушёл бы в **чужую** таблицу по правилу `31001+`; ненулевой младший байт делает метки разных лучей неотличимыми; ноль — не метка вовсе, цепочка `pick` отбрасывала бы каждый свой же помеченный пакет. Все четыре случая конфигурация ловит сама и говорит в лог |
| `rule_pref_base` | `30000` | База приоритетов: правило луча — `+i`, его страховка — `+500+i`, общая страховка — `+511`. **Цена ошибки:** ниже 200 правило встало бы впереди правила Forkop (`pref 105`), и tproxy не увидел бы свой пакет; выше 30489 страховки залезли бы в полосу obfmesh-balance (`31000+i`) и obfmesh (`32000+i`) — два пакета стали бы снимать правила друг друга. Что применилось на самом деле, показывает `status` |
| `table_name` | `obfmesh_onwatch` | Имя своей таблицы nft; уходит в командную строку `nft`, поэтому всё, кроме букв, цифр и подчёркивания, отбрасывается целиком |

Чаще остальных трогают два, и оба — на первых сутках:

```sh
uci set obfmesh-onwatch.main.dry_run=0        # снять сухой ход, когда список похож на правду
uci set obfmesh-onwatch.main.require_domain=0 # заворачивать и цели без имени — понимая цену
uci commit obfmesh-onwatch
```

#### 3.9.5 Диагностика: почему не заворачивает

Проверять по порядку — от того, что не стоит ничего, к тому, что стоит форков и времени. Первые
четыре причины закрывают почти все случаи.

**1. Сухой ход не снят.** Самая частая. В журнале при этом есть события `dry-run` — «завернул бы, и
вот почему», а `hot4` пуст:

```sh
obfmesh-onwatch status | grep dry_run
grep -c '"event":"dry-run"' /etc/obfmesh-onwatch/journal.jsonl
```

**2. Пакет выключен или процессы не работают.**

```sh
obfmesh-onwatch status | grep -E 'enabled|daemon|захват DNS'
logread -e obfmesh-onwatch | tail -20
```

Захват DNS не работает — имён не будет, а при `require_domain = 1` не будет и заворачивания. Демон
не работает — улики копятся в ядре, но решать их некому.

**3. У цели нет имени.** Это не поломка, а объявленная граница: запрос ушёл мимо dnsmasq (DoH в
браузере) либо клиент взял ответ из своего кеша.

```sh
jsonfilter -i /var/run/obfmesh-onwatch/state.json -e '@.last_reason'   # доля кандидатов без имени
grep -c . /var/run/obfmesh-onwatch/domains.map
obfmesh-onwatch pin rutracker.org                                      # лечится только так
```

**4. Цель в карантине.** Значит, проба уже была и завернуть нечего: либо сайт открывается напрямую
(403 и 451 отдают совершенно рабочие сайты, `rc 60` — негодный сертификат, и луч этого не чинит),
либо не открывается и через луч — тогда он лежит целиком.

```sh
obfmesh-onwatch list | sed -n '/В КАРАНТИНЕ/,/^$/p'
grep '"event":"quarantine"' /etc/obfmesh-onwatch/journal.jsonl | tail -5
```

Причина каждой записи лежит в поле `reason`, и там стоит код возврата `curl`: прямой пробы, а если
дело дошло до второй — то и пробы через луч. Снять карантин с одной цели командой нельзя, `unpin`
его не трогает; вынимается элемент из множества:

```sh
nft delete element inet obfmesh_onwatch quarantine4 { 104.21.0.1 . 443 }
```

**5. Имя уже в обходе.** За именем резолвер отдавал FakeIP — значит, его уже разбирает sing-box, и
заворачивать второй раз нечего. Улика на настоящем адресе при этом законна: сенсор поймал чужое
соединение к тому же адресу.

```sh
grep '^rutracker.org ' /var/run/obfmesh-onwatch/fakeip.list
```

**6. Сработал предохранитель.** Кандидатов за такт больше 32 — это не «сайт заблокировали», а
массовое явление: сменилась политика DPI, сломался отпечаток, кто-то сканирует сеть. Решатель не
проверяет и не заворачивает ничего 300 секунд: завернуть весь дом в один луч хуже, чем не завернуть
ничего. В логе — строка `предохранитель`, в журнале — запись `limit`.

**7. Упёрлись в `max_hot`** — строка в логе и запись `limit` в журнале, цель остаётся оборванной.

**8. Нет живых лучей.** Проверять цель нечем и заворачивать некуда; цель остаётся кандидатом и
дождётся возвращения луча. В логе — «живых лучей нет».

```sh
obfmesh status
obfmesh-onwatch check | tail -20
```

**9. Улик нет вовсе.** Смотреть надо в ядро — что видит сенсор:

```sh
nft list set inet obfmesh_onwatch rst_hits     # инжектированные RST по парам адрес.порт
nft list set inet obfmesh_onwatch syn_watch    # SYN без ответа
nft list counters table inet obfmesh_onwatch   # hot_new и hot_pkts
```

Пусто при живом обрыве — блокировка выглядит иначе, чем ловит пакет: подмена DNS, страница-заглушка
с кодом 200, блокировка по QUIC, порт не 80 и не 443, IPv6. Такие цели заворачиваются руками через
`pin`, а если и это не помогает — цель не блокируется, а лежит.

**10. Множества улик существуют, но всегда пусты.** Признак того, что в ядре у них нет `flags
dynamic`: `nft -c` такое пропускает молча, а без флага ядро не даст правилам в них писать. Пакет
проверяет это и на своём тексте, и на том, что принято ядром, — строка ищется в логе:

```sh
logread -e obfmesh-onwatch | grep dynamic
```

**11. Всё завёрнуто, а сайт по-прежнему оборван.** Заворот действует на новые соединения — нужно
обновить страницу. Уже открытое соединение помнит свой луч (или его отсутствие) в `ct mark`, и
рвать его нечем.

#### 3.9.6 Перенос накопленного в секцию `on_test` Forkop

Пакет — временная мера. Постоянное место домена, который надо обходить, — секция Forkop: там он
разбирается целиком, со всеми поддоменами и всеми адресами, включая те, которых захват DNS не
видел, и уходит в обход через sing-box, минуя нашу таблицу, метку, SNAT и страховки.

**Это единственная операция во всей фиче, которая требует `uci commit forkop` и рвёт связь всему
дому.** Механика та же, что в 3.7 и 3.8.2: commit шлёт procd событие `config.change`, procd
перезапускает Forkop, тот пересобирает `/etc/sing-box/config.json` и поднимает sing-box заново —
все соединения через прокси обрываются разом, у всех устройств. Поэтому делается в тихое время,
одним заходом, накопленным списком, и с точкой отката.

**Почему это не автоматическое** — четыре причины, и первая из них главная:

1. **Цена.** Автомат делал бы этот обрыв по каждой опознанной цели, то есть несколько раз в день и
   в произвольный момент — посреди звонка и посреди загрузки. Ровно от этого пакет и заведён:
   «ронять нельзя интернет на постоянной основе».
2. **Правка уезжает в чужую конфигурацию и на флеш.** Журнал решений и `pinned.list` — наши, их
   правит и стирает пакет. `/etc/config/forkop` ведёт человек, и снимок для отката тоже он.
3. **Решение шире наблюдения.** Пакет знает пару адрес.порт и одно имя, увиденное в захвате; в
   секцию попадает домен целиком. Автомат перевёл бы туда адрес CDN по одному случайному имени —
   вместе с сотнями чужих сайтов на том же адресе.
4. **Ошибка стоит по-разному.** Ложный заворот в onwatch истекает сам через `hot_ttl` и снимается
   одной командой. Ложная строка в секции Forkop живёт, пока её не уберут руками, и уводит в обход
   весь домен.

Что переносить — имена из журнала, а не адреса:

```sh
{ cat /etc/obfmesh-onwatch/journal.jsonl
  cat /var/run/obfmesh-onwatch/journal.pending; } 2>/dev/null |
	grep -E '"event":"(wrap|dry-run)"' |
	sed -n 's/.*"domain":"\([^"]*\)".*/\1/p' | sort -u

obfmesh-onwatch export        # закреплённое и завёрнутое ПРЯМО СЕЙЧАС — готовыми командами pin;
                              # истёкшее и то, что было завёрнуто вчера, живёт только в журнале
```

Список читается **глазами**: адрес CDN, попавший в журнал по одному имени, в секцию не переносится,
а имя, которое человек не узнаёт, — повод сперва посмотреть причину в журнале.

Дальше — снимок, правка, один commit. Точка отката — копия файла `/etc/config/forkop`, снятая
прямо сейчас, права `600`, живёт только в `/root`: в нём `subscription_urls` с токеном в пути
(порядок и оговорки — 3.7).

```sh
SNAP=/root/forkop.bak.$(date +%Y%m%d-%H%M%S)
( umask 077; cp /etc/config/forkop "$SNAP" ); chmod 600 "$SNAP"
md5sum /etc/config/forkop "$SNAP"          # хеши совпали — снимок полный

uci -q get forkop.on_test.domain           # что в секции сейчас
md5sum /etc/sing-box/config.json           # хеш ДО
pidof sing-box                             # pid ДО

cur="$(uci -q get forkop.on_test.domain)"
uci set forkop.on_test.domain="$cur
rutracker.org
rutor.info"

uci commit forkop                          # ЗДЕСЬ рвутся все соединения через прокси у всего дома
sleep 10
```

Проверка ровно та же, что в 3.7, плюс одна своя:

```sh
md5sum /etc/sing-box/config.json           # хеш обязан измениться
pidof sing-box                             # pid обязан стать другим
nslookup rutracker.org 127.0.0.42          # адрес обязан быть из 198.18.0.0/15 — это FakeIP
```

Хеш прежний или pid прежний — триггер не сработал или собрал конфигурацию до правки: только тогда
`/etc/init.d/forkop restart` и те же три команды ещё раз.

**После переноса — убрать за собой в onwatch.** Закрепление руками надо снять: демон возвращает
закреплённую пару в `hot4` на каждом обслуживании и будет заворачивать её по адресам, которые в
карте имён живут ещё шесть часов.

```sh
obfmesh-onwatch unpin rutracker.org
```

Запись, поставленную детектором, снимать не нужно: клиент теперь получает за это имя FakeIP, к
настоящему адресу больше не ходит, и запись истечёт сама через `hot_ttl`. Что пакет про домен
замолчал, видно так:

```sh
grep '^rutracker.org ' /var/run/obfmesh-onwatch/fakeip.list   # имя увидено с FakeIP
obfmesh-onwatch list                                          # его нет ни в завёрнутом, ни в
                                                              # закреплённом
```

#### 3.9.7 Откат и снятие пакета

Три состояния, и путать их не надо.

**Мягко.** `hot4` пустеет, новые соединения идут напрямую, а таблица, правила лучей и их страховки
остаются в ядре: по ним доживают уже завёрнутые соединения — луч записан у них в `ct mark`. Так
работают `obfmesh-onwatch off`, `enabled = 0` и `/etc/init.d/obfmesh-onwatch stop`. **Само это
состояние не снимается никогда** — у onwatch нет аналога `OMB_SOFT_GRACE` балансировщика, и мягкая
остановка держится до `teardown` или до перезагрузки роутера.

```sh
obfmesh-onwatch off
obfmesh-onwatch status | grep -E 'nft table|завёрнуто'
```

**Жёстко.** Таблица, правила `30000+i` и страховки `30500+i` с `30511` уходят целиком. Завёрнутое
соединение после этого не рвётся, а **виснет** до таймаута приложения (причина — выше, 3.9.3).
Команда об этом предупреждает.

```sh
obfmesh-onwatch teardown
ip rule show | grep -E '^30[05][0-9][0-9]:' || echo 'правил и страховок нет'
nft list table inet obfmesh_onwatch 2>/dev/null || echo 'таблицы нет'
```

**Снять пакет целиком** — файлами, потому что в базе apk его нет:

```sh
obfmesh-onwatch off                        # сначала enabled = 0, иначе демон соберёт всё заново
obfmesh-onwatch teardown                   # при установке пакетом это делает prerm, тут звать некому
/etc/init.d/obfmesh-onwatch stop; /etc/init.d/obfmesh-onwatch disable
rm -f /etc/init.d/obfmesh-onwatch /usr/bin/obfmesh-onwatch /etc/config/obfmesh-onwatch
rm -rf /usr/lib/obfmesh-onwatch /var/run/obfmesh-onwatch
# /etc/obfmesh-onwatch — журнал решений и закреплённые цели. Их писал оператор, и удаление пакета
# не повод стирать объяснение, почему сайт ездил через луч. Убирать — отдельным решением
```

Порядок среди трёх пакетов: **onwatch снимается первым**, затем obfmesh-balance, затем obfmesh
(6.1). Он читает состав лучей у obfmesh, и снятый раньше времени obfmesh оставил бы его правила
смотреть в пустые таблицы. По той же причине в init-скриптах он стартует последним (`START=97`) и
останавливается первым (`STOP=8`): перестать заворачивать надо, **пока лучи ещё живы**, иначе между
его остановкой и остановкой obfmesh цепочка успеет отправить новое соединение в луч, у которого
таблицы маршрутов уже нет.

Обновление устроено как у соседей: установщик останавливает сервис мягко, заменяет файлы
переименованием и поднимает обратно; таблица и правила замену переживают, `prerm` при
`PKG_UPGRADE=1` не зовётся.

---

## 4. Число обфускаторов

### 4.1 Сколько лучей

Число лучей — одна настройка на сервере, всё остальное подтягивается само: сервер поднимает
недостающие `swg{i}` и обфускаторы, повышает `config_version`, роутер получает событие по SSE,
скачивает новый бандл и меняет только разницу. Диапазон 1..10.

С роутера (нужен админ-ключ в uci):

```sh
uci set obfmesh.main.api_key='<админ-ключ сервера>'
uci commit obfmesh
obfmesh spokes            # прочитать
obfmesh spokes 3          # изменить
obfmesh status
```

С сервера:

```sh
KEY=$(sudo cat /etc/obfmesh/admin.key)
curl -s -X PATCH http://127.0.0.1:8080/api/settings \
     -H "X-API-Key: $KEY" -H 'Content-Type: application/json' \
     -d '{"spokes":3}'
```

Или в панели, раздел «Настройки лучей».

Уменьшение гасит только лишние лучи вместе с их таблицами и правилами: работающие не трогаются, их
ключи остаются в базе, поэтому обратное увеличение не меняет уже выданные бандлы. Установленные
сессии на оставшихся лучах не рвутся — маршруты этих лучей не меняются вообще.

**Сколько ставить.** На NanoPi R3S проверено замерами: два луча — 359–373 Мбит/с, три —
210–222 Мбит/с. Третий луч отнимает скорость, потому что процессор роутера уже загружен на
92–98 %. Увеличивать число лучей имеет смысл только на более сильном клиенте и только с замером до
и после.

### 4.2 Как убедиться, что луч действительно работает

«Интерфейс поднят» и «трафик идёт» — разные вещи. Каждый луч проверяется отдельно и полностью.

На роутере, для луча i (пример для первого):

```sh
ip -4 addr show owg1                   # 10.77.1.2/30, и этого адреса нет больше нигде
ip route show table 51821              # ровно одна строка: default dev owg1 mtu 1400
ip rule show | grep 51821              # 32001: from 10.77.1.2 lookup 51821
ping -c3 -I owg1 10.77.1.1
curl --interface owg1 -s https://ifconfig.me; echo
wg show owg1 transfer                  # оба счётчика растут
```

На сервере:

```sh
wg show swg1 allowed-ips               # 10.77.1.2/32 — один адрес, и только он
wg show swg2 allowed-ips               # 10.77.2.2/32
ip route show | grep 10.77.            # только подсети swg{i}, без nexthop-конструкций
```

Ключевой момент: адрес клиента принадлежит ровно одному лучу с обеих сторон. Второй адрес в
AllowedIPs, многопутевой маршрут к клиенту, устройство `agg0`, непустая таблица `51820` — это следы
схемы 1.1.0, а не рабочая конфигурация. После обновления с 1.1.0 стоит убедиться, что их не
осталось:

```sh
ip link show agg0 2>/dev/null && echo 'ОСТАТОК 1.1.0: agg0'
ip route show table 51820 | grep . && echo 'ОСТАТОК 1.1.0: таблица 51820'
ip rule show | grep -E '^30000:' && echo 'ОСТАТОК 1.1.0: правило oif agg0'
ip -4 addr show | grep '10\.77\.0\.' && echo 'ОСТАТОК 1.1.0: агрегатный адрес'
```

**Реактивность.** На роутере:

```sh
obfmesh status | grep watcher          # running
logread -e obfmesh | tail -20          # ошибок чтения bundle.json быть не должно
```

Меняем число лучей на сервере (4.1) и через десяток секунд смотрим на роутере:

```sh
obfmesh status                         # config_version вырос сам, без ручного obfmesh sync
```

Если новое значение появляется только после ручного `obfmesh sync` — watcher до сервера не
доходит: смотрите `logread -e obfmesh` сразу после старта сервиса.

---

## 5. Диагностика

### 5.1 Куда смотреть

| Где | Команда или файл | Что там |
|---|---|---|
| сервер | `journalctl -u obfmesh-server -n 80` | API, reconcile, ошибки оркестратора |
| сервер | `/var/log/obfmesh/obf{i}.log` | вывод серверного обфускатора луча i |
| сервер | `obfmesh-ctl status` | лучи, интерфейсы, живость обфускаторов без HTTP |
| сервер | `obfmesh-ctl reconcile` | привести систему к состоянию из базы и показать, что менялось |
| роутер | `obfmesh status`, `obfmesh status --json` | сводка |
| роутер | `obfmesh logs -n 100`, `logread -e obfmesh` | apply.sh и watcher |
| роутер | `/var/run/obfmesh/obf-c{i}.log` | вывод клиентского обфускатора луча i |
| роутер | `/var/run/obfmesh/state.json` | что реально применено |
| роутер | `ip route show table 51820+i`, `ip rule show` | маршруты лучей |
| роутер | `obfmesh-balance status`, `obfmesh-balance check` | балансировка: веса, карта в ядре, привязки Forkop |
| роутер | `obfmesh-balance logs -n 100`, `logread -e obfmesh-balance` | демон весов и транзакции nft |
| роутер | `nft list table inet obfmesh_balance` | карта весов и цепочки балансировщика |
| роутер | `obfmesh-onwatch status`, `obfmesh-onwatch check` | детектор: лучи, завёрнутые цели, счётчики захвата DNS |
| роутер | `obfmesh-onwatch list` | что завёрнуто, что в карантине, что закреплено — с именами |
| роутер | `/etc/obfmesh-onwatch/journal.jsonl` и `/var/run/obfmesh-onwatch/journal.pending` | журнал решений: почему эта цель поехала через луч (переживает перезагрузку) |
| роутер | `obfmesh-onwatch logs -n 100`, `logread -e obfmesh-onwatch` | решатель и захват DNS |
| роутер | `nft list set inet obfmesh_onwatch rst_hits` | улики детектора: инжектированные RST по парам адрес.порт |
| роутер | `/var/run/obfmesh-onwatch/domains.map` | карта «адрес → имя» из захвата DNS, единственный источник имён |

Секреты в логи и в вывод команд не попадают: на их месте `<есть>`.

### 5.2 Симптом → куда смотреть

| Симптом | Что проверить |
|---|---|
| `obfmesh status` пуст, бандла нет | `obfmesh logs -n 50`; `server_url`, `token`, `ca_file`; `curl` из шага 3.3 |
| `GET /api/bundle` отвечает 401 | токен не тот или клиент удалён; заведите клиента заново |
| `GET /api/bundle` отвечает 409 | на сервере ещё нет лучей или не задан `external_host`: `obfmesh-ctl reconcile` |
| Луч поднят, рукопожатия нет | путь до `48200+i`: `tcpdump -ni any udp port 48201` на сервере; INPUT-правила |
| Обфускатор `DEAD` на роутере | `cat /var/run/obfmesh/obf-c1.log`; частая причина — занятый порт `13301` |
| Обфускатор падает на сервере | `tail -50 /var/log/obfmesh/obf1.log`, `journalctl -u obfmesh-server -n 80` |
| Рукопожатия идут, трафика нет | маскировка: `grep masking /etc/obfmesh/obf1.conf` и `/var/run/obfmesh/obf-c1.conf` — значения должны совпадать байт в байт |
| Через `owg{i}` не ходит ничего | проверка из 4.2 целиком: адрес, таблица, правило, AllowedIPs на сервере |
| Скорость как у одного луча | все потребители сидят на одном интерфейсе: `wg show owg2 transfer` не растёт — раскладка из 3.7 |
| Скорость около 210 вместо 359 Мбит/с | настройка роутера не применилась: проверка из 3.6 (`taskset`, `rps_cpus`, `netdev_budget`) |
| В `rps_cpus` снова `f`, а не `3` | маску сбило редкое сетевое событие (перерегистрация устройства, чужой `sysctl -p`). Постоянного писателя нет: watcher вернёт своё на ближайшем тике, окно — до шести минут. Мимо ядер обфускаторов это стоит около пятой части полосы (3.6) |
| Замер полосы сервиса с роутера вдвое ниже клиентского | так и должно быть: `curl` на роутере отбирает такты у sing-box и обфускаторов — снято 157 против 348 в ту же минуту. С роутера честно меряется только сам луч (3.5), полоса сервиса — с клиента (3.8.4) |
| Скорость упала после добавления третьего луча | так и должно быть на R3S: 210–222 против 359–373, см. 4.1 |
| Один поток не разгоняется выше ~180 Мбит/с | это потолок одной TCP-сессии, лечения нет (SPEC, «Что проверено и отвергнуто») |
| Ответы не приходят на тот луч, с которого ушли | `wg show swg{i} allowed-ips` — там должен быть ровно один адрес клиента |
| Часть секций потребителя потеряла интернет | их луч погашен уменьшением `spokes`; перевесить `bind_interface` на живой |
| Изменения на сервере не подхватываются | строка `watcher` в `obfmesh status`; `logread -e obfmesh` |
| Сервис «останавливается» и встаёт обратно | `ls /var/run/obfmesh/admin-down` — watcher уважает этот маркер |
| После `systemctl stop` на сервере туннели живы | так задумано: `KillMode=process`. Гасить — `obfmesh-ctl teardown` |
| Панель отдаёт 502/504 на `/api/events` | буферизация прокси: `flush_interval -1` у Caddy, `proxy_buffering off` у nginx |
| После обновления пропал токен | конфиг перезаписан мимо пакетного менеджера; `/etc/config/obfmesh` обязан быть в `conffiles`, см. PACKAGING.md, 2.5 |
| Балансировщик включён, а трафик весь в базовом луче | `obfmesh-balance check`: секция на `routing_mark` или на `bind_interface` небазового луча в цепочку не попадает; `mark_base` должен совпасть с `route.default_mark` |
| `obfmesh-balance on` не включает пакет и ругается на секцию | перевод не доведён: названная секция сидит на `bind_interface` интерфейса базового луча. Отказ штатный и приходит не только от `on`, но и от `apply`, `weights` и каждого такта демона — довести перевод по 3.8.2 или перевесить секцию на небазовый луч. Опция `allow_bind_interface` отказ снимает, но секцию не чинит (3.8.3) |
| Один сервис подвисает и переоткрывается, остальные в норме | его секция привязана через `bind_interface` к интерфейсу базового луча, а балансировка включена (`obfmesh-balance` до 1.1.0, где включение ещё не проверялось): часть соединений уводится на чужой луч и гибнет в страховке. В `/proc/net/nf_conntrack` у них `packets=0` в обе стороны и метка небазового луча. Лечится переводом секции (3.8.2) |
| `obfmesh-balance status`: `KERNEL` пуст или расходится с `WEIGHT` | демон не работает или транзакция nft не прошла: `obfmesh-balance logs -n 50` |
| Веса ходят туда-сюда при `mode = auto` и живых лучах | так вёл себя прежний регулятор на приросте байт: 90/10 → 75/25 → 60/40 и обратно каждые четыре такта. В нынешнем цель — константа, ровные доли, и под нагрузкой веса стоят (проверено сорок секунд подряд 50/50). Смотреть строку `веса` в `status`: `adapt = conns` двигает цель законно, `adapt = off` — нет |
| Переброшенное соединение не отвечает | нет SNAT или нет правила `31000+i`: `nft list table inet obfmesh_balance`, `ip rule show \| grep 3100` |
| Правила `31000+i` пропадают после каждого `obfmesh apply` | на роутере obfmesh старше 1.3.0: `obfmesh version`. Начиная с 1.3.0 чужие правила в своих таблицах он не трогает (приложение В) |
| Правила `31000+i` пропадают сами по себе | их снял кто-то ещё; демон вернёт их на следующем такте — `obfmesh-balance logs -n 50` покажет, ставил ли он их заново |
| Часть соединений вдруг никуда не идёт, а базовый луч работает | сработала страховка `31500+i`: таблица луча пуста, и помеченный пакет отбрасывается вместо утечки в WAN. Проверить `ip route show table 51820+i` и `obfmesh status` — луч погашен или obfmesh остановлен |
| После `obfmesh-balance teardown` соединения не рвутся, а висят | так и работает: conntrack пережил снятие таблицы, `nf_nat` доигрывает старый SNAT, пакет уходит в базовый луч с чужим адресом источника и дропается пиром. Ждать таймаута приложения или переоткрыть соединение |
| Пакет выключили, а таблица и правила на месте | это мягкая остановка, и она штатная. Работающий демон снимет их через `OMB_SOFT_GRACE` (600 с); демона нет — снимать некому, `obfmesh-balance teardown` |
| Сайт оборван, а `obfmesh-onwatch` его не заворачивает | по порядку из 3.9.5: сухой ход не снят, у цели нет имени (DoH), цель в карантине, имя уже в обходе через sing-box, сработал предохранитель. Первым делом — `obfmesh-onwatch status \| grep dry_run` и события `dry-run` в журнале |
| `obfmesh-onwatch` завернул цель, а сайт всё равно оборван | заворот действует на новые соединения: обновить страницу. Старое помнит своё в `ct mark`, а рвать его нечем — утилиты `conntrack` в прошивке нет (3.9.3) |
| Множества `rst_hits` и `syn_watch` всегда пусты | у них в ядре нет `flags dynamic`, и `nft -c` такое пропускает молча: `logread -e obfmesh-onwatch \| grep dynamic`. Либо блокировка выглядит иначе, чем ловит пакет — подмена DNS, заглушка с кодом 200, QUIC, порт не 80 и не 443 (3.9.5) |
| Карта имён пуста, а захват DNS работает | сборка `tcpdump` не печатает владельца записи: прогнать команду из 3.9.2 руками — в строке обязано быть имя (`github.com. A 140.82.121.4`), а не только `A 140.82.121.4`. Ключ `-v` обязателен, `-q` ломает разбор, `-vv` подмешивает glue-записи из additional |
| `obfmesh-onwatch` завернул то, чего не просили | цель снимается `unpin`, а причина лежит в журнале решений полем `reason` — там коды возврата обеих проб `curl`. Пока список не похож на правду, работать надо в сухом ходу (3.9.2) |
| Завёрнутые сайты все разом перестали открываться | `spoke_mode` — фиксированный луч, и он лёг: помеченный пакет ловит страховка `blackhole`, то есть цель пропадает совсем. `obfmesh-onwatch check` скажет это прямо; лечится `spoke_mode = auto` (3.9.4) |
| Пакет выключили (`obfmesh-onwatch off`), а таблица и правила на месте | так и задумано, и само это не снимется: аналога `OMB_SOFT_GRACE` у onwatch нет. Убрать — `obfmesh-onwatch teardown` (3.9.7) |

### 5.3 Ручные операции на роутере

```sh
obfmesh sync                # забрать бандл и применить, если изменился
obfmesh apply               # применить сохранённый бандл (снимает administrative down)
obfmesh down                # погасить всё, оставив сервис включённым
obfmesh logs -f             # хвост лога
obfmesh version             # версии пакета, бандла, обфускатора
```

`obfmesh down` ставит маркер `/var/run/obfmesh/admin-down`, иначе watcher поднял бы всё обратно на
ближайшем тике. Маркер живёт в tmpfs: после перезагрузки роутера туннель поднимется снова.

Балансировщик, если он установлен:

```sh
obfmesh-balance status          # веса, живость лучей, что лежит в карте nft
obfmesh-balance check           # почему не работает: привязки Forkop, метка, правила
obfmesh-balance apply           # пересобрать ruleset и правила прямо сейчас
obfmesh-balance weights 1:70 2:30   # ручные веса, переводит mode в manual
obfmesh-balance off              # мягко: новые соединения базовым лучом, Forkop не трогается.
                                 # Таблица, правила 31000+i и страховки 31500+i ОСТАЮТСЯ — по ним
                                 # доживают переброшенные соединения; работающий демон снимет их
                                 # через OMB_SOFT_GRACE (600 с)
obfmesh-balance teardown         # жёстко и сразу: снять таблицу, правила и страховки.
                                 # Переброшенные соединения после этого виснут до таймаута
                                 # приложения — сбросить им conntrack нечем
```

Детектор недоступных сайтов, если он установлен:

```sh
obfmesh-onwatch status           # лучи, завёрнутые цели, счётчики захвата DNS
obfmesh-onwatch check            # почему не заворачивает: инструменты, пробы nft, метки, лучи
obfmesh-onwatch list             # что завёрнуто, что в карантине, что закреплено
obfmesh-onwatch pin <имя>        # завернуть насовсем — единственное лечение слепоты к DoH
obfmesh-onwatch unpin <имя>      # снять закрепление и вывести цель из обхода (карантин не трогает)
obfmesh-onwatch export           # закреплённое и завёрнутое готовыми командами pin
obfmesh-onwatch off              # мягко: hot4 пуст, новые соединения напрямую. Таблица, правила
                                 # 30000+i и страховки 30500+i ОСТАЮТСЯ и сами не снимутся
obfmesh-onwatch teardown         # жёстко: снять всё. Завёрнутые соединения после этого виснут
```

---

## 6. Удаление начисто

### 6.1 Роутер

Пакеты ставились установщиком (3.1, 3.8.1 и 3.9.1), поэтому `apk del` их не видит и снимать нечего:
`apk info -e obfmesh obfmesh-balance obfmesh-onwatch` молчит. Удаление — файлами, и то, что при
установке пакетом сделали бы `prerm` и `postrm`, здесь делается явно.

```sh
# 1. Вернуть потребителям прежний выходной интерфейс (обратный шаг 3.7).
obfmesh-onwatch off              # если ставился: снимается ПЕРВЫМ из трёх — он читает состав
                                 # лучей у obfmesh и гасится раньше соседей (STOP=8)
obfmesh-onwatch teardown         # таблица nft, правила 30000+i, страховки 30500+i и 30511;
                                 # завёрнутые соединения после этого виснут до таймаута приложения
/etc/init.d/obfmesh-onwatch stop; /etc/init.d/obfmesh-onwatch disable
rm -f /etc/init.d/obfmesh-onwatch /usr/bin/obfmesh-onwatch /etc/config/obfmesh-onwatch
rm -rf /usr/lib/obfmesh-onwatch /var/run/obfmesh-onwatch
# /etc/obfmesh-onwatch (журнал решений и закреплённые цели) оставляется: их писал оператор

obfmesh-balance off              # если ставился: сначала enabled = 0, иначе демон успевает
                                 # собрать таблицу и правила заново
obfmesh-balance teardown         # снимает таблицу nft, правила 31000+i и страховки 31500+i;
                                 # при установке пакетом это делает prerm, а тут звать некому
/etc/init.d/obfmesh-balance stop; /etc/init.d/obfmesh-balance disable
rm -f /etc/init.d/obfmesh-balance /usr/bin/obfmesh-balance /etc/config/obfmesh-balance
rm -rf /usr/lib/obfmesh-balance /var/run/obfmesh-balance

/etc/init.d/obfmesh stop
/etc/init.d/obfmesh disable
rm -f /etc/init.d/obfmesh /usr/bin/obfmesh /etc/config/obfmesh
rm -rf /usr/lib/obfmesh /etc/obfmesh /var/run/obfmesh
```

Порядок обязателен: и obfmesh-balance, и obfmesh-onwatch читают состав лучей у obfmesh, и снятый
раньше времени obfmesh оставил бы правила `31000+i` и `30000+i`, ведущие в пустые таблицы. По той же
причине в init-скриптах они стартуют после него и останавливаются раньше: `START` 95 → 96 → 97,
`STOP` 8 → 9 → 10, то есть детектор гасится первым, а obfmesh последним. Окно между `STOP=9` и `STOP=10` — то, в котором obfmesh уже сделал
`ip route flush table 51820+i`, а помеченные соединения ещё живы, — закрывают страховки
`31500+i`: помеченный пакет там отбрасывается, а не уходит в WAN под MASQUERADE.

`stop` гасит лучи, чистит таблицы `51820+i` и снимает правила. Удаление руками должно дополнительно
стереть `/etc/obfmesh/bundle.json` (в нём приватные ключи) и весь `/var/run/obfmesh` (в нём
`curl.rc` с токеном) — при установке пакетом это делает `postrm`, поэтому оба каталога стоят в
списке `rm -rf` выше.

Проверка, что не осталось следов:

```sh
ip link show owg1 2>/dev/null || echo 'лучи убраны'
ip route show table 51821                # пусто
ip rule show | grep -E '^320[0-9][0-9]:' || echo 'правил нет'
ip rule show | grep -E '^31[05][0-9][0-9]:' || echo 'правил и страховок балансировщика нет'
nft list table inet obfmesh_balance 2>/dev/null || echo 'таблицы балансировщика нет'
ip rule show | grep -E '^30[05][0-9][0-9]:' || echo 'правил и страховок детектора нет'
nft list table inet obfmesh_onwatch 2>/dev/null || echo 'таблицы детектора нет'
ls /etc/obfmesh /var/run/obfmesh 2>&1    # нет таких каталогов
pgrep -af wg-obfuscator || echo 'обфускаторов нет'
ping -c2 8.8.8.8                         # домашний интернет жив
```

Настройки, которые делал `tune.sh` (governor, RPS, `netdev_budget`), живут до перезагрузки и
никакого следа в конфигурации не оставляют. Нужно вернуть их немедленно — перезагрузите роутер или
верните значения руками.

Пакет `ip-full` ставился отдельно и остаётся: его могли использовать другие сервисы. Убирать —
только сознательно.

### 6.2 Сервер, установка пакетом

```sh
apt-get purge -y obfmesh-server
```

`prerm` останавливает сервис, гасит обфускаторы и удаляет интерфейсы `swg{i}`, `purge` стирает
`/etc/obfmesh` (админ-ключ, конфиги с ключом обфускации), `/var/lib/obfmesh` (база) и
`/var/log/obfmesh`. Без `purge` база и ключи остаются — переустановка их подхватит.

### 6.3 Сервер, установка из исходников

```sh
systemctl disable --now obfmesh-server
obfmesh-ctl teardown --interfaces
rm -rf /opt/obfmesh /etc/obfmesh /var/lib/obfmesh /var/log/obfmesh /run/obfmesh
rm -f /etc/systemd/system/obfmesh-server.service /etc/logrotate.d/obfmesh /usr/local/sbin/obfmesh-ctl
rm -rf /etc/systemd/system/obfmesh-server.service.d
systemctl daemon-reload
```

### 6.4 Общее для обоих способов

Правила NAT и FORWARD общие для всех лучей и ни `purge`, ни `teardown` их не снимают — иначе
удаление одного экземпляра сломало бы соседний. Снимаются вручную:

```sh
iptables -t nat -D POSTROUTING -s 10.77.0.0/16 ! -d 10.77.0.0/16 -j MASQUERADE
iptables -D FORWARD -s 10.77.0.0/16 -j ACCEPT
iptables -D FORWARD -d 10.77.0.0/16 -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
for p in $(seq 48201 48210); do iptables -D INPUT -p udp --dport $p -j ACCEPT 2>/dev/null; done
```

Блок обратного прокси из конфигурации Caddy/nginx убирается отдельно, вместе с перезагрузкой
прокси.

---

## Приложение А. Порты, адреса, таблицы

| Что | Где | Значение |
|---|---|---|
| Обфускатор луча i, вход | сервер, наружу | `udp/(port_base + i)`, по умолчанию `48200 + i` |
| WireGuard луча i | сервер, 127.0.0.1 | `udp/(51820 + i)` |
| API | сервер, 127.0.0.1 | `tcp/8080` |
| Обфускатор луча i, вход | роутер, 127.0.0.1 | `udp/(13300 + i)` |
| WireGuard луча i | роутер, 127.0.0.1 | `udp/(51820 + i)` |
| Адрес сервера в луче i | — | `10.77.{i}.1/24` |
| Адрес клиента в луче i | — | `10.77.{i}.{4k+2}/30`, k — слот клиента |
| AllowedIPs пира клиента на `swg{i}` | сервер | `10.77.{i}.{4k+2}/32`, ровно один адрес |
| Таблица маршрутов луча i | роутер | `route_table_base + i`, по умолчанию `51820 + i` |
| Приоритет правила луча i | роутер | `rule_pref_base + i`, по умолчанию `32000 + i` |
| MTU туннеля | обе стороны | `1400`, на интерфейсе и в маршруте |

`port_base` меняется через `PATCH /api/settings`; диапазон обфускаторов не должен пересекаться с
`51820±10` и с портом API — валидация это проверяет и отказывает.

## Приложение Б. Переменные окружения сервера

Задаются в drop-in юнита (`/etc/systemd/system/obfmesh-server.service.d/10-local.conf`), после
правки — `systemctl daemon-reload && systemctl restart obfmesh-server`.

| Переменная | По умолчанию | Что делает |
|---|---|---|
| `OBFMESH_EXTERNAL_HOST` | адрес из маршрута по умолчанию | внешний адрес сервера; читается только при создании строки настроек |
| `OBFMESH_OBFUSCATOR_BIN` | `/usr/local/bin/wg-obfuscator` | путь к бинарнику; там же, читается один раз |
| `OBFMESH_MANAGE_INPUT` | `0`, в юните `1` | вести ли правила INPUT для портов обфускаторов |
| `OBFMESH_STOP_PROCESSES` | `0` | гасить ли обфускаторы при остановке сервиса |
| `OBFMESH_RECONCILE_INTERVAL` | `300` | период фонового reconcile, секунды; `0` — выключить |
| `OBFMESH_API_PORT` | `8080` | порт API; менять вместе с `ExecStart` и блоком прокси |
| `OBFMESH_DB` | `/var/lib/obfmesh/obfmesh.db` | файл базы |
| `OBFMESH_LOG_LEVEL` | `INFO` | уровень лога сервиса |
| `OBFMESH_OBF_VERBOSE` | `1` | подробность лога обфускаторов, 0..4 |
| `OBFMESH_ADMIN_KEY_FILE` | `/etc/obfmesh/admin.key` | файл админ-ключа |
| `OBFMESH_ADMIN_KEY` | — | админ-ключ значением; перекрывает файл |
| `OBFMESH_DOCS` | `0` | `1` открывает `/docs` и `/openapi.json` |

## Приложение В. Опции uci на роутере

Секция `config obfmesh 'main'`, файл `/etc/config/obfmesh` (права 600 — в нём токен; объявлен в
`conffiles`, обновление пакета его не перезаписывает).

| Опция | По умолчанию | Что делает |
|---|---|---|
| `enabled` | `1` | `0` гасит всё и оставляет сервис в простое |
| `server_url` | — | база API, например `https://host/obfmesh` |
| `token` | — | клиентский токен (Bearer) |
| `obfuscator_bin` | `/usr/bin/wg-obfuscator` | путь к бинарнику |
| `poll_interval` | `30` | период опроса и housekeeping, секунды, минимум 5 |
| `watch` | `sse` | `sse` или `poll` |
| `log_level` | `info` | `debug`, `info`, `warn`, `error` |
| `tune` | `1` | применять настройку роутера (`tune.sh`): ядра обфускаторов, RPS, `netdev_budget`, governor |
| `route_table_base` | `51820` | база номеров таблиц: луч i получает `route_table_base + i` |
| `rule_pref_base` | `32000` | база приоритетов правил: луч i получает `rule_pref_base + i` |
| `tls_insecure` | `0` | `1` отключает проверку сертификата (только для диагностики) |
| `ca_file` | — | PEM с корневым сертификатом сервера |
| `api_key` | — | админ-ключ; нужен для `obfmesh spokes N` |
| `sse_max_time` | `900` | предельное время одного SSE-соединения, секунды |
| `http_timeout` | `20` | таймаут обычных REST-вызовов, секунды |
| `keepalive` | `25` | `persistent-keepalive` на лучах, секунды |
| `obfuscator_mask_value` | — | перекрыть тип маскировки из бандла; трогать только для проверки гипотез |
| `obfuscator_extra_args` | — | дополнительные аргументы обфускатору |

Опций `agg_iface`, `agg_address`, `route_table`, `rule_pref` и `fwmark` в 1.2.0 нет: единого
агрегирующего устройства и единой таблицы туннеля больше не существует. Если раньше трафик
заворачивался в туннель по метке, теперь метка направляется в конкретный луч своим правилом,
поставленным рядом с obfmesh:

```sh
ip rule add fwmark 0x1 lookup 51821 pref 30001     # метка 0x1 → первый луч
```

**Приоритет выбирается не любой свободный.** Заняты две полосы obfmesh-balance (раздел 3.8):
`31001..31010` — его правила лучей, `31501..31510` — их страховки `blackhole`. Правило соседа с тем
же приоритетом ему мешает. Полоса `32001..32010` — самого obfmesh. Ниже `31000` и выше правила
Forkop `pref 105` места достаточно, поэтому пример и стоит на `30001`.

Такое правило obfmesh не ведёт, не снимает при остановке и не сносит на `apply`: он считает и
чистит только правила своего приоритета `32000+i`, а всё остальное, что смотрит в его таблицы, —
чужая настройка (SPEC.md, инвариант 8). Раньше обещание нарушалось кодом: первый же `apply`
уносил такое правило вместе со своими, и заметить это можно было только по пропавшему трафику.
Исправлено вместе с выпуском obfmesh-balance, который на этом механизме и построен.

Два исключения, оба намеренные.

**Селектор, дословно совпавший со своим.** С 1.3.0 obfmesh узнаёт свои правила по селектору:
`from <адрес луча> lookup <таблица луча>` и `oif <устройство луча> lookup <таблица луча>`. Такую
строку он снимает на **любом** приоритете — иначе смена `rule_pref_base` оставляла бы прежнюю пару
в ядре навсегда. Поэтому заворот в луч ставится по метке, а не повторением адреса луча в `from`:
`ip rule add from 10.77.1.2 lookup 51821 pref 30001` — это дословно правило obfmesh, и оно уйдёт
на ближайшем `apply`. Правило с любым другим селектором (`fwmark`, `iif`, чужой `from`) остаётся
на месте.

**Общая таблица `51820` схемы 1.1.x** чистится целиком, вместе с чужими правилами. Ею эта версия
не пользуется, и любое смотрящее в неё правило ведёт в никуда.

Готовый потребитель механизма — пакет obfmesh-balance (раздел 3.8): он ставит правила
`fwmark 0x{i}00/0xff00 lookup 51820+i` на приоритетах `31000+i`, страховки
`fwmark 0x{i}00/0xff00 blackhole` на `31500+i` и держит живыми и те, и другие.

Своим он считает правило, у которого сошлись оба признака: приоритет ровно `31000+i` и селектор
ровно `fwmark 0x{i}00/0xff00`; страховку — так же строго, приоритет ровно `31500+i` и то же
действие `blackhole`. Правило по метке, поставленное рядом на другом приоритете, под это не
подходит и остаётся (SPEC.md, раздел про инварианты obfmesh-balance).

В таблицу базового луча он правил не ставит — метка базового луча не меняется, уводить нечего. Но
свой след там снимает: луч, который раньше был небазовым, оставляет правило `31000+i`, и при смене
`base_spoke` оно уходит по тем же двум признакам. «Не ставит» и «не чистит» — разные вещи, и
рассчитывать на второе нельзя.

## Приложение Г. Опции uci пакета obfmesh-balance

Секция `config balance 'main'`, файл `/etc/config/obfmesh-balance` (объявлен в `conffiles`).
Секретов в нём нет, но есть настройка живого роутера, поэтому обновление пакета его не
перезаписывает. Демон перечитывает конфигурацию на каждом такте — `reload` нужен только чтобы
поднять его, если он не запущен.

| Опция | По умолчанию | Что делает |
|---|---|---|
| `enabled` | `0` | `0` — пакет не создаёт ни таблицы, ни правил. Включать после `obfmesh-balance check` |
| `base_spoke` | `1` | индекс луча, на адрес которого биндятся секции Forkop, `1..10` — столько лучей вообще бывает. Его трафик не перемаршрутизируется и не SNAT-ится. На интерфейс этого луча не должна быть привязана ни одна секция Forkop через `bind_interface`: адрес источника у неё тот же, под фильтр цепочки она попадает, а перемаршрутизацию не переживает — пока такая секция есть, применить ruleset отказываются и `on`, и `apply`, и `weights`, и такт демона (3.8.3) |
| `allow_bind_interface` | `0` | снимает защиту от секций Forkop на `bind_interface` интерфейса базового луча. При `1` (а также `yes`, `true`, `on`) `on`, `apply`, `weights` и такт демона перестают отказывать, и балансировка включается поверх таких секций — их соединения, уведённые на чужой луч, гибнут в страховке `31500+i`. Любое другое значение, включая опечатку, защиту оставляет. Снятие видно: `warn` в логе и отдельная строка в `check`, где секция проблемой быть не перестаёт (3.8.3) |
| `mode` | `auto` | `auto` — веса ведёт демон: РОВНЫЕ доли `100/N` между живыми лучами, лёгший луч получает ноль и отдаёт свою долю живым, а отклонения без нужды не бывает (см. `adapt`); `manual` — берутся из `weights` и не пересчитываются; `off` — сервис демона не поднимает, а уже работающий переходит в мягкую остановку (карта весов на базовый луч) и через `OMB_SOFT_GRACE` снимает своё целиком |
| `adapt` | `off` | адаптация в `mode = auto`, при `manual` и `off` не значит ничего. `off` — ровные доли, и для двух лучей равной ёмкости это же оптимум: вес управляет только новыми соединениями, открытое переехать не может. `conns` — поверх ровных долей поправка по числу АКТИВНЫХ соединений на луче, счёт снимается с `/proc/net/nf_conntrack` по метке соединения. Порог поправки — два сигма (`перекос² > всего соединений`), цена — форк и проход по всей таблице conntrack раз в `interval` (20 мс при 141 записи, дальше ~0.08 мс на запись). Не `off` и не `conns` — строка в лог и `off`. Прирост байт в решение не входит ни при каком значении: он диагностика, колонка `DELTA` в `status` |
| `interval` | `5` | период такта, секунды, `1..3600` |
| `min_share` | `10` | минимальная доля живого луча, проценты, `0..100`. Если `min_share × N > 100`, порог урезается до `100/N`. При `adapt = off` ни на что не влияет: ровная доля `100/N` и так не ниже него — работать он начинает только с `adapt = conns`, ограничивая поправку снизу |
| `max_step` | `15` | предел изменения веса за такт, процентные пункты, `1..100`. Он же скорость возвращения поднявшегося луча |
| `weights` | — | список ручных весов для `mode = manual`: `list weights '1:60'` (по индексу луча) или `list weights '40'` (позиционно, по возрастанию индекса). Формы смешиваются; сумма приводится к 100. Остаток сначала делится поровну между живыми лучами, которых в списке НЕТ, и только то, что осталось после них, достаётся базовому: при двух лучах `1:60` даёт 60/40, а не 60/0. Явный ноль — не «луч не назван»: `2:0` оставляет лучу 2 ноль. Мёртвый луч получает ноль независимо от списка |
| `rule_pref_base` | `31000` | база приоритетов: правило небазового луча i — `rule_pref_base + i`, его страховка `blackhole` — `rule_pref_base + 500 + i`. Допустимо `200..31489`: ниже 200 правило встало бы впереди правила Forkop `pref 105` и tproxy не увидел бы свой пакет; выше — страховка задела бы полосу obfmesh (`32000+i`). Негодное значение всегда заменяется на 31000, но говорит об этом не всякое: `1..199` и `31490..32700` дают строку `warn` в логе, а не число, ноль и всё, что больше 32700, подменяются молча — их отсекает общая проверка числа до разбора границ |
| `mark_base` | `0x08000000` | база меток; обязана совпадать с `route.default_mark` в конфигурации sing-box. Запрещены бит `0x04000000`, любой бит из байта `0x0000FF00`, значение шире 32 бит и ноль (при нулевой базе цепочка `out` поймала бы весь НЕпомеченный трафик с адреса базового луча). Такое значение отвергается со строкой в логе, берётся `0x08000000` |
| `table_name` | `obfmesh_balance` | имя своей таблицы nft. Имя, где есть что-то кроме букв, цифр и подчёркивания, отвергается целиком — берётся `obfmesh_balance` |

Диапазоны у `base_spoke`, `interval`, `min_share` и `max_step` проверяются одинаково и **молча**:
пустая строка, минус, `abc`, `5s`, `0x10` и любое число за границей диапазона дают значение по
умолчанию из этой же таблицы, и ни строки в лог при этом не пишется. Опечатку в них видно только по
`obfmesh-balance status`: он печатает то, что реально прочитано, — строками `base spoke : 1`,
`mode : auto (interval 5s, min_share 10%, max_step 15)` и `веса : ровные доли между живыми лучами
(adapt = off)`. Проверять — там, а не в `uci show`.

Настройками не являются и в uci не выносились (все они — константы `lib.sh`):

| Константа | Значение | Что решает |
|---|---|---|
| `OMB_HS_MAX` | `180` | сколько секунд по монотонным часам сырое `latest-handshakes` может стоять на месте, прежде чем луч объявлен мёртвым |
| `OMB_MUTE_TICKS` | `3` | сколько тактов подряд `tx` растёт при стоящем `rx`, чтобы вывести односторонний луч, не дожидаясь протухшего рукопожатия |
| `OMB_MUTE_SECS` | `30` | и сколько при этом секунд: `interval` настраивается, а `persistent-keepalive` на лучах 25 секунд |
| `OMB_REDISCOVER` | `300` | период полного перечитывания состава лучей у obfmesh; между ними состав перечитывается по смене набора адресов на `owg*` |
| `OMB_SOFT_GRACE` | `600` | сколько выключенный пакет держит мягкую остановку, прежде чем работающий демон снимет таблицу, правила и страховки. Отсчёт идёт только в цикле демона и только при `enabled = 0` или `mode = off`; демона нет — мягкое состояние держится до перезагрузки или до `obfmesh-balance teardown`. Читается из окружения, чтобы тестам не ждать десять минут |

Приоритеты страховок отдельной опцией тоже не задаются: смещение `+500` от `rule_pref_base`
зашито (`OMB_RULE_GUARD_OFF`), и верхняя граница `rule_pref_base` посчитана именно по ним.

Рабочий каталог — `/var/run/obfmesh-balance` (700): `state.json` — что применено последним тактом,
`ruleset.md5`, `weights.sig` и `skeleton.sig` — подписи, по которым демон решает, надо ли вообще
трогать nft, `daemon.pid`.

## Приложение Д. Опции uci пакета obfmesh-onwatch: границы и негодные значения

Секция `config onwatch 'main'`, файл `/etc/config/obfmesh-onwatch` (объявлен в `conffiles`).
Что каждая опция делает и чем платят за ошибку — раздел 3.9.4; здесь то, чего там нет: границы и
поведение на негодном значении. Разница важна: часть опций подменяется **молча**, и увидеть
подмену можно только в `obfmesh-onwatch status`, который печатает прочитанное, а не записанное.

| Опция | По умолчанию | Границы | Что с негодным значением |
|---|---|---|---|
| `enabled` | `0` | `1`, `yes`, `true`, `on` — включено | всё остальное молча читается как `0` |
| `dry_run` | `1` | `0`/`no`/`false`/`off` либо `1`/`yes`/`true`/`on` | **строка `error` в лог и `1`**: опечатка не имеет права начать заворачивать живой трафик |
| `require_domain` | `1` | `0`/`no`/`false`/`off` — выключено | всё остальное молча читается как `1` |
| `interval` | `5` | `1..3600` | молча `5` |
| `spoke_mode` | `auto` | `auto` либо номер луча `1..10` | строка `error` в лог и `auto` — и на мусоре, и на числе вне диапазона |
| `hot_ttl` | `2h` | число секунд либо число с `s`/`m`/`h`/`d`, `60s..7d` | не разобрано — `error` в лог и `2h`; вне границ — `warn` и `2h` |
| `quarantine_ttl` | `12h` | там же, `60s..7d` | не разобрано — `error` и `12h`; вне границ — `warn` и `12h` |
| `rst_threshold` | `2` | `1..100` | молча `2` |
| `syn_threshold` | `3` | `1..100` | молча `3` |
| `probe_timeout` | `5` | `1..60` | молча `5` |
| `max_hot` | `256` | `1..4096` — столько мест в множестве `hot4` | молча `256` |
| `mark_base` | `0x02000000` | `0x`-запись или десятичная; запрещены ноль, шире 32 бит, бит `0x04000000` (tproxy Forkop), ненулевой байт `0x0000FF00` (номер луча у obfmesh-balance) и ненулевой младший байт | каждый случай — своя строка `error` с объяснением и `0x02000000`. Совпадение с меткой сокетов sing-box `0x08000000` не запрещено: `warn` в лог, значение остаётся |
| `rule_pref_base` | `30000` | `200..30489`: ниже правило встало бы впереди правила Forkop `pref 105`, выше страховки залезли бы в полосу obfmesh-balance | `200..30489` — принимается; `1..199` и `30490..32700` — `warn` и `30000`; ноль, мусор и всё больше `32700` подменяются **молча** общей проверкой числа |
| `table_name` | `obfmesh_onwatch` | буквы, цифры, подчёркивание | строка `error` и `obfmesh_onwatch`: имя уходит в командную строку `nft` |

Смена `rule_pref_base` на работающем роутере убирает прежнюю полосу целиком: применённое значение
запоминается в `/var/run/obfmesh-onwatch/rule_pref.base`, и при его смене правила и страховки со
старых приоритетов снимаются. Файл живёт в tmpfs — после перезагрузки убирать нечего, `ip rule` её
не переживают тоже.

Настройками не являются и в uci не выносились (константы `lib.sh`, `nft.sh`, `daemon.sh` и
`sniff.sh`) — опция, которую никто не крутит, это ещё один способ сломать работающее опечаткой:

| Константа | Значение | Что решает |
|---|---|---|
| `OWN_PORTS` | `80 443` | порты, за которыми следит цепочка `sense`; отпечаток RST снят на них |
| `OWN_RST_TTL` | `600` | сколько живёт улика об инжектированном RST — связь между сорванным соединением и доменом |
| `OWN_SYN_TTL` | `10` | окно ожидания ответа на SYN: десять секунд — уже молчание, а не медленный сервер |
| `OWN_HS_MAX` | `180` | возраст рукопожатия, после которого луч считается лёгшим (keepalive на лучах 25 с) |
| `OWN_MAINT_IDLE` / `OWN_MAINT_HOT` | `30` / `15` | период обслуживания в покое и когда завёрнуто хоть что-то. `interval` плюс второе — верхняя граница жизни пропавшего правила |
| `OWN_PROBE_MAX` / `OWN_PROBE_BUDGET` | `3` / `2` | сколько целей проверяется за такт и сколько `probe_timeout` на это отводится: цикл, стоящий в `curl`, не отвечает ни на сигнал, ни на новую улику |
| `OWN_PROBE_COOLDOWN` | `600` | как часто одна и та же цель имеет право на повторную пробу; совпадает со сроком жизни улики |
| `OWN_STORM_CAND` / `OWN_STORM_HOLD` | `32` / `300` | предохранитель: больше стольких кандидатов за такт — это не блокировка сайта, а массовое явление, и заворачивание останавливается на `OWN_STORM_HOLD` секунд |
| `OWN_DOMAIN_TTL` / `OWN_DOMAINS_MAX` | `21600` / `4096` | сколько живёт запись карты имён и сколько их держится: адрес CDN за шесть часов достаётся другому имени |
| `OWN_JOURNAL_GAP` / `_MAX` / `_KEEP` | `60` / `131072` / `500` | не чаще раза в минуту на флеш, файл не растёт выше 128 КиБ, при обрезке остаются последние 500 записей |
| `OWN_RULE_GUARD_OFF` | `500` | насколько приоритет страховки отстоит от приоритета правила луча; по нему же посчитана верхняя граница `rule_pref_base` |
| `OWN_LAN_IF` / `OWN_WAN_IF` | `br-lan` / `eth0` | между какими устройствами ходит завёрнутый трафик |
| `OWN_NEVER4` | список | что не заворачивается никогда: RFC-диапазоны, FakeIP `198.18.0.0/15`, сеть провайдера `37.9.41.0/24` и адреса резолверов — завернув резолвер, роутер отрезал бы себя от имён |

Каталоги два, и они разного назначения. `/etc/obfmesh-onwatch` (700) переживает перезагрузку:
`journal.jsonl` — журнал решений, `pinned.list` — закреплённые руками цели. `/var/run/obfmesh-onwatch`
(700) живёт в tmpfs: `state.json` — что применено последним тактом, `sniff.json` — счётчики захвата
и объявленная слепота к DoH, `domains.map` — карта «адрес → имя», `fakeip.list` — имена, за которыми
резолвер отдавал FakeIP, `journal.pending` — записи, ещё не уехавшие на флеш, `ruleset.md5`,
`spokes.sig` и `rule_pref.base` — подписи применённого, `daemon.pid`, `sniff.pid` и два pid-файла
procd. Обнуление tmpfs при перезагрузке — это правильно: адреса CDN за ночь меняются целиком.
